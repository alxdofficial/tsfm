# Adversarial debug sweep — verified findings

_Source: `adversarial-debug-sweep` workflow (wf_45856d75-5cf), 100 agents, 2026-07-09._
_51 findings confirmed after adversarial verification (from 80 candidates across 20 audit targets)._

Each is a checklist item; check off as fixed.


## DATA (26)

### [HIGH] DATA-1: TRAIN dataset "hhar" (Heterogeneity Activity Recognition) —  — preprocessing
- [ ] **Issue:** resample_stream has no guard against timestamp gaps, so a single corrupt Creation_Time fabricates a 36.4-hour fake 'standing' stream. User a / device nexus4_1 / gt=stand has only 55,163 real accel rows spanning 278s of real motion, but one gap of 130,931.8s (36.4h) between two timestamps makes t[-1]-t[0]=131,230s. resample_stream computes n=int(131230*50)+1=6,561,505 grid points and np.interp linearly interpolates across the gap (99.79% of samples are interpolated fill), producing 103,414 near-static overlapping windows for a_stand alone — 59% of the whole hhar corpus (103,414/175,253). 99.4% of user-a-standing windows are dead-flat (accel-magnitude peak-to-peak < 0.05 m/s^2); the QA plot shows 3 of 6 random panels as perfectly straight lines. Every other (user,activity) stream is a normal ~1,000-2,000 windows (~20-45 min).
  - **Evidence:** datascripts/hhar/convert.py:116-134 resample_stream: `duration=float(t[-1]); ... n=int(duration*rate)+1; ... out[c]=np.interp(grid,t,...)` with no gap/outlier guard. Raw scan of Phones_accelerometer.csv (user a, gt=stand, device nexus4_1): rows=55163, span=36.45h, max_gap=130931.8s, real coverage 278s=0.21%. Per-(user,activity) window counts: a/stand=103,414 (~36.8h implied) vs every other stream 865-2,066. Flat-fraction: user-a-standing 2323/2337=99.4% flat vs everything-else 8.8%.
  - **Fix:** In resample_stream (datascripts/hhar/convert.py), stop interpolating across timestamp gaps. Preferred: split each (user,device,activity) stream into contiguous segments wherever np.diff(t) exceeds a few sample periods (e.g. >0.5s at 50Hz target) and resample+window each contiguous segment independently; discard segments shorter than WINDOW_SIZE. Equivalent alternatives: mask/drop grid points whose nearest real sample is farther than a tolerance, or reject Creation_Time outliers (using Arrival_Time, which spans only 298s here, as a sanity cross-check since HHAR Creation_Time is known-unreliable). Then delete data/hhar/sessions (also needed to clear the 318,543 stale on-disk dirs the non-idempotent converter never removes) and re-run convert.py; the corpus should collapse from 175,253 to ~72k legitimate windows, with a/stand dropping from 103,414 to a normal ~1–2k. This also fixes the misleading '226h available' hhar hours budget in TRAIN_HOURS_PER_DATASET.

### [HIGH] DATA-2: TRAIN dataset "hhar" (Heterogeneity Activity Recognition) —  — sampling/labels
- [ ] **Issue:** The fabricated stream produces pathological label + subject imbalance that the loader propagates into training. standing = 112,112/175,253 = 64% of labels (vs 9.6k-16.4k for the other five classes); user a = 110,048/175,253 = 63% of sessions (every other user ~7-9k). The training cap MAX_SESSIONS_PER_DATASET['hhar']=round(24h*3600/2.56)=33,750 is a UNIFORM random subsample of labels.json, so ~59% of the 33,750 selected windows (~19,900) are the fabricated user-a flat-standing stream. group_balanced sampling (temperature=0, 20x cap) rebalances GROUPS across the corpus but cannot fix that within-hhar 'standing' is ~91.5% fabricated flats, nor that hhar's subject diversity collapses to one mega-subject.
  - **Evidence:** labels.json value distribution: standing=112,112, walking_downstairs=16,429, walking_upstairs=16,391, walking=10,546, sitting=10,145, cycling=9,630. Users in ids: a=110,048 vs b-i in 7,144-8,772. Cap math in semantic_alignment_train.py:174,178 (`"hhar": 24.0`, `_hours_to_session_cap`) = 33,750. Also affects _create_splits (multi_dataset_loader.py:363): hhar has 9 subject-groups, val=int(9*0.15)=1 group, so the 63%-of-data user 'a' lands wholesale in exactly one of train/val/test, destabilizing whichever split it enters.
  - **Fix:** Root cause is the hhar conversion (finding #1): re-convert so per-class and per-subject counts are balanced, then re-derive the hours-budget cap from the corrected mean session length. Additionally, make the training path honor the dataset_config subsampling block that already specifies method='stratified', stratify_by=['activity','subject'] for hhar — currently multi_dataset_loader.py:355-358 does a uniform random.shuffle()+truncate that ignores it entirely. Replace the uniform cap with stratified (subject,activity) subsampling so no single subject/class dominates the capped set. After re-conversion verify per-class and per-subject counts are ~balanced. As a lighter interim guard even before re-conversion, stratifying the cap would prevent ~59% of the 33,750 capped hhar windows from being one subject's near-flat standing stream and would restore hhar's subject diversity in the subject-disjoint split.

### [MEDIUM] DATA-3: TRAIN dataset "hhar" (Heterogeneity Activity Recognition) —  — preprocessing
- [ ] **Issue:** The gap-interpolation artifact is NOT confined to user-a-standing: localized smaller gaps fabricate dead-flat windows inside the DYNAMIC stair classes, mislabeling motionless interpolation as stair-climbing. walking_upstairs is 23.1% flat and walking_downstairs is 15.5% flat (accel-magnitude peak-to-peak < 0.05 m/s^2 over 2.56s), which is physically impossible for genuine stair activity. The QA plot panel 6 (hhar_c_stairsup_125949, label=walking_upstairs) is a perfectly straight line. With ~16k windows each, this is roughly 2,500-3,800 fabricated flat windows actively teaching the model that a motionless signal means 'climbing stairs'.
  - **Evidence:** Stratified flat analysis over 4,000 random sessions: walking_upstairs 81/350=23.1% flat, walking_downstairs 59/380=15.5% flat, while walking=0.0%, cycling=0.4%, sitting=0.9% are clean. test_output/data_qa/hhar_sessions.png panel 6 shows a flat-line walking_upstairs window for subject c.
  - **Fix:** Root cause: datascripts/hhar/convert.py resample_stream() does np.interp(grid, t, ...) over the full stream duration, so any large inter-sample time gap in the raw HHAR clock is filled with a straight-line ramp; when a gap spans a full 128-sample window it fabricates a dead-flat/linear window that keeps the class label. Fix in the converter: detect raw gaps where dt between consecutive Creation_Time samples exceeds a threshold (e.g. > 3x the local nominal period, or an absolute > ~0.15-0.2s), and either (a) split the physical stream into contiguous segments at those gaps and window each segment separately, or (b) mark grid points that fall inside a gap and drop any window overlapping an interpolated-gap region. This same gap-guard also removes the standing/#1 flat windows. Additionally add the finding's QA gate: in the converter (and the data_qa plot script) reject or flag any non-stationary-labeled window whose accel-magnitude dynamic range is below a physical floor (e.g. ptp < 0.05 m/s^2), and add a linearity check (linear-fit R^2 ~ 1.0 with ~0 residual) to catch interpolation ramps specifically. Re-run the converter and confirm stair-class flat fraction drops from ~16-17% to ~0%.

### [MEDIUM] DATA-4: TRAIN dataset "hhar" (Heterogeneity Activity Recognition) —  — plumbing
- [ ] **Issue:** Session-count bookkeeping is inconsistent across five places and 143,290 on-disk session dirs are orphans (no labels.json entry, silently ignored by the loader). On disk: 318,543 session dirs / data.parquet files. labels.json (what multi_dataset_loader actually iterates): 175,253 keys. dataset_config num_sessions: 146,557. dataset_config subsampling.target_sessions: 15,000. Training cap actually applied: 33,750. All five differ. The known flag ('318,543 on disk vs config 146,557') missed that labels.json is a distinct third number (175,253) and is the count that governs training. The 143,290 disk-only dirs (labels-only not-on-disk=0, disk-only=143,290) are dead weight (likely stale from a prior convert run — data/raw/hhar_reconvert.log exists) costing ~143k orphan parquet files.
  - **Evidence:** glob of data/hhar/sessions/*/=318,543; len(labels.json)=175,253; benchmark_data/dataset_config.json datasets.hhar.num_sessions=146,557 and subsampling.hhar.target_sessions=15,000; semantic_alignment_train.py cap=33,750. Set difference: disk-only=143,290, labels-only=0. Loader builds its session list from labels.json keys (multi_dataset_loader.py:338-349), so orphans never train but inflate disk and confuse accounting.
  - **Fix:** Clear data/hhar/sessions/ and re-run datascripts/hhar/convert.py so on-disk dirs == labels.json (175,253), eliminating the 143,290 orphans and ~2 GB of dead parquets. Then update benchmark_data/dataset_config.json datasets.hhar.num_sessions from the stale 146,557 to the actual converted count (175,253) so export_raw.py:283 no longer exports wrong 'num_sessions_original' metadata to baselines. For subsampling.hhar.target_sessions=15,000: note it is not dead — it is consumed by export_raw.py:319-324 for baseline data export — so either keep it (and document that it governs only the baseline export path, while HALO training uses the TRAIN_HOURS_PER_DATASET hours-budget cap of 33,750) or remove it if baseline subsampling is no longer desired; do not describe it as unused. Optionally add a convert.py guard that wipes the sessions/ dir before writing to prevent future reconvert orphaning.

### [MEDIUM] DATA-5: pamap2 TRAIN dataset — end-to-end (datascripts/pamap2/conver — channel-desc / preprocessing / plumbing
- [ ] **Issue:** All 12 PAMAP2 orientation channels (hand/chest/ankle_ori_1..4) are fed to the model as input channels even though the dataset documents orientation as INVALID, and the channel-text conditioning that HALO relies on labels them 'constant placeholder' — which is factually wrong for 8 of the 9 subjects (their ori quaternions vary), and dead/all-zero for the 9th.
  - **Evidence:** multi_dataset_loader.py:419 `IMU_PATTERNS = ['acc', 'gyro', 'mag', 'ori']` -> 'ori' matches *_ori_1..4. Real loader output (datasets=['pamap2'], val split, subject106) feeds 48 IMU channels incl. all 12 ori, each with desc '...Orientation quaternion component N ... (INVALID: orientation turned off in PAMAP2; constant placeholder) (sampled at 100Hz, 2.0s window)' while the ori DATA has std 0.05-0.55 (not constant). Per-subject scan: subj101 ori=(1,0,0,0) constant AND ori_2/3/4 all-zero (dead); subj102-109 ori varies, e.g. subj102 hand_ori[0]=[0.175,0.335,-0.494,-0.783]. So 12/48 (25%) of model input channels are documented-invalid data with a contradictory description.
  - **Fix:** Two independent fixes, both valid:

A) Exclude orientation from model input. Drop 'ori' from IMU_PATTERNS in BOTH multi_dataset_loader.py:419 (get_sample) and :747 (get_channel_counts) so orientation is excluded exactly like heart_rate/temp already are. This removes 12/48 (25%) documented-INVALID channels from PAMAP2 and also shrinks the channel-bucket batch key consistently. Optionally strip ori columns in datascripts/pamap2/convert.py so the parquet no longer stores them. NOTE: get_channel_counts must stay in sync with get_sample or ChannelBucketBatchSampler will mis-bucket — change both call sites together.

B) If orientation is intentionally kept (e.g. as a robustness/noise channel), correct the factually wrong manifest text: replace "constant placeholder" with an accurate description, e.g. "(INVALID: PAMAP2 orientation is documented unreliable; values vary but are not calibrated — do not treat as a physical quaternion)". Regenerate data/pamap2/manifest.json (the on-disk manifest text also differs from the current converter output, so re-run the converter or patch the manifest directly). Preferred: do A (drop ori) since there is no evidence the invalid orientation helps and it removes the contradiction at the source; keep B as the minimum if ori is retained.

### [MEDIUM] DATA-6: pamap2 TRAIN dataset — end-to-end (datascripts/pamap2/conver — channel-desc / paper-fidelity / reproducibility
- [ ] **Issue:** The committed converter datascripts/pamap2/convert.py does NOT reproduce the on-disk data/pamap2/manifest.json: 30 of 52 channel descriptions differ. Re-running it would silently strip the load-bearing 'values in m/s^2 including gravity' unit annotation from all 18 acc channels and regress the top-level description from '12 protocol activities' back to '18 activities'.
  - **Evidence:** Replicated convert.py create_manifest() (convert.py:196-226,230) and diffed vs data/pamap2/manifest.json. convert.py emits acc desc 'Acceleration x-axis (±16g scale) from wrist-mounted IMU' vs on-disk 'Acceleration x-axis (±16g sensor range, values in m/s^2 including gravity) from wrist-mounted IMU'; ori wording also differs; top desc '...9 subjects performing 18 activities...' vs on-disk '...12 protocol activities...'. 30/52 channel descriptions differ; the data actually contains only 12 activities (labels.json).
  - **Fix:** Update convert.py create_manifest() to emit the current richer acc descriptions ("±{scale}g sensor range, values in m/s^2 including gravity"), the "Orientation quaternion component N ... turned off in PAMAP2" wording, and the "9 subjects performing 12 protocol activities" top-level text, so the committed converter regenerates the committed/on-disk manifest byte-for-byte. Then regenerate and commit data/pamap2/manifest.json so all three (converter output, HEAD, working tree) agree; also reconcile the ACTIVITIES dict/comment so it no longer implies 18 activities are produced from Protocol/. Alternatively, if the manifest is intentionally hand-authored, vendor it and add a note that it is not converter-generated — but the factually-incorrect "18 activities" must be corrected either way.

### [MEDIUM] DATA-7: dsads (TRAIN dataset) — converter, manifest/labels/config, l — labels / dataset-config
- [ ] **Issue:** dataset_config.json's DSADS activity vocabulary disagrees with the emitted labels: it lists "lying_side", but the converter and labels.json emit "lying_right_side" (480 sessions). The config activity string is simply wrong (not a missing class), so any path built from config['datasets']['dsads']['activities'] gets a label that never exact-matches the data.
  - **Evidence:** benchmark_data/dataset_config.json:464 -> "lying_side"; data/dsads/labels.json has 'lying_right_side' x480 (and 'lying_back' x480); datascripts/dsads/convert.py:34 maps a04->'lying_right_side'. Consumers reached: benchmark_data/scripts/export_raw.py:280 writes metadata.json activities (default ALL_DATASETS = train+zero_shot, so dsads is in scope), and evaluate_crosshar.py:391 / evaluate_limubert.py:180 / grouped_zero_shot.py sort config activities.
  - **Fix:** In benchmark_data/dataset_config.json:464 change the dsads activities entry "lying_side" to "lying_right_side" so the config vocabulary matches datascripts/dsads/convert.py and data/dsads/labels.json. Then regenerate the derived artifacts that copied the stale string: benchmark_data/raw/dsads/metadata.json, benchmark_data/processed/limubert/{global_label_mapping.json, dsads/mapping.json}, and benchmark_data/processed/tsfm_eval/dsads/metadata.json (re-run export_raw.py and preprocess_limubert.py for dsads), so activity_to_idx no longer maps lying_right_side to -1.

### [MEDIUM] DATA-8: TRAIN dataset "kuhar" (KU-HAR) — converter, data/kuhar/{mani — channel-desc / preprocessing (units+gravity)
- [ ] **Issue:** KU-HAR accelerometer is gravity-removed / orientation-independent (linear acceleration, m/s^2), but manifest.json and dataset_config describe it as a plain 'Smartphone accelerometer X-axis (waist)' with no gravity/orientation disclosure, the SAME channel text used for the corpus's gravity-INCLUSIVE datasets (mhealth, pamap2, dsads, hhar, wisdm, recgym). The 'accelerometer (waist)' text token thus means contradictory physics across datasets, so the CLIP text encoder cannot disambiguate and static postures (standing 1768, sitting 1711, lying 1741 windows) carry no gravity-direction cue while being aligned to distinct posture strings. In the headline spectral_temporal path per-patch z-score masks the scale/DC (latent there) but it is active for text conditioning and for the documented physical-Hz filterbank redesign, which does NOT z-score.
  - **Evidence:** 400-session audit: static-posture accel |mean| DC = standing 0.019, sitting 0.019 median, lying 0.013, talking_sitting 0.016 m/s^2 (vs ~9.8 if gravity present); only 21.8% gravity-like. Visual QA test_output/data_qa/kuhar_sessions.png: lying(1062)/sitting(1040) show all 3 accel axes oscillating around 0 with no ~1g offset. reference notes: accel m/s^2, dataset orientation-independent. manifest.json:9 'Smartphone accelerometer X-axis (waist)'.
  - **Fix:** Rewrite KU-HAR acc channel descriptions in data/kuhar/manifest.json (and the converter's create_manifest in datascripts/kuhar/convert.py so it regenerates correctly) to state gravity-removed / linear acceleration, mirroring uci_har body_acc semantics, e.g. 'Smartphone body acceleration X-axis from accelerometer, waist (gravity removed, m/s^2)'. This makes the channel token corpus-consistent so the CLIP text encoder no longer reads the same 'accelerometer (waist)' string as gravity-present (mhealth/dsads/hhar/wisdm/recgym) and gravity-removed (kuhar). Do NOT add gravity back (KU-HAR removed it deliberately). Note this only corrects the text/metadata contradiction and matters most for the non-z-scored filterbank redesign; it cannot restore a gravity-direction cue for KU-HAR's static postures (standing/sitting/lying are inherently near-zero flat signals aligned to distinct posture strings), so it does not fix the underlying static-posture indistinguishability — a broader consideration for whether to weight/keep those windows.

### [MEDIUM] DATA-9: TRAIN dataset "kuhar" (KU-HAR) — converter, data/kuhar/{mani — sampling-rate usage
- [ ] **Issue:** manifest/dataset_config/channel-text assert a flat 100 Hz, but subject 1101 (1520 sessions = 8.7% of ALL kuhar windows) is consistently ~126 Hz and ~7% of sessions overall exceed 110 Hz (p95=126 Hz). The headline spectral_temporal path cuts patches at the declared 100 Hz and the sampled-at-100Hz suffix is the ONLY rate cue the text encoder gets, so 1101 content is ~26% frequency-mis-scaled vs its own label; the physical-Hz filterbank redesign would mis-map bands by the same 26%.
  - **Evidence:** s1101_* span-based rate ~126.0-126.3 Hz across sessions (s1101_jumping_0892_0000: 1276 samp / 10.108 s). 400-session audit: p95=126.1, 7.2% >110 Hz, only 1101 mean >110. dataset_config kuhar sampling_rate_hz=100; convert.py:59 SAMPLE_RATE=100.0.
  - **Fix:** At convert time, estimate a ROBUST per-file rate rather than assuming 100 Hz: use the within-burst/trimmed-median inter-sample interval (or a span estimate after discarding outlier gaps), and clamp/flag files whose timestamps are corrupted (e.g., s1101 running sessions showing 10–63000 Hz). Then either (a) resample every file to a common 100 Hz with resample_poly (uniformizing the bursty 126 Hz stream at the same time), or preferably (b) persist the true per-file/per-session rate into manifest.json / a per-session field so both the channel-text builder (multi_dataset_loader.py:588/615) and the physical-Hz filterbank (metadata['sampling_rate_hz']) see ~126 Hz for those sessions instead of a hard-coded 100. Option (b) is the better long-term fix because it keeps native rate for the rate-invariant tokenizer redesign; option (a) is the simpler stopgap for the current CNN/spectral checkpoint. Whichever is chosen, drop or repair the corrupted-timestamp s1101 sessions rather than deriving a bogus rate from them.

### [MEDIUM] DATA-10: TRAIN dataset "kuhar" (KU-HAR) — converter, data/kuhar/{mani — sampling / fairness (subject imbalance)
- [ ] **Issue:** Subject 1101 alone is 1520/17374 = 8.7% of kuhar windows, 7.8x the median subject (165) and 2.9x the next-largest (1022, 527), from 111 distinct raw files vs ~31 for a normal subject, all at the anomalous ~126 Hz. This one outlier dominates the dataset; with the subject-disjoint split, 1101 lands wholly in one of train/val/test and skews whichever split it falls in.
  - **Evidence:** labels.json per-subject: 1101=1520 (8.7%), 1022=527, 1014=494; median 165, mean 195, min 19, max 1520 over 89 subjects. 1101 has 111 distinct (activity,file-index) raw files vs 31 (subj 1038)/33 (subj 1008). 1101 is also the lone 11xx entry in dataset_config test_subjects.
  - **Fix:** Investigate the raw source of subject 1101: it is out of the 1001-1090 range, runs at ~126 Hz (not 100 Hz), and comprises 111 files/1520 windows — almost certainly a mis-parsed or aggregated raw file (the converter parses subject from the filename prefix via re.match(r'(\d+)_', ...) at datascripts/kuhar/convert.py:137, which will silently absorb any anomalously-named file into one ID). Either (a) quarantine/drop 1101 (and reconcile the two missing in-range subjects 1012/1059), or (b) cap its per-subject window contribution toward the per-subject median (~165) in the loader. Separately, add a per-subject cap in multi_dataset_loader (kuhar is uncapped, TRAIN_HOURS_PER_DATASET['kuhar']=None) so no single subject can dominate a subject-disjoint split. Also fix the silent rate mislabel: manifest.json declares 100 Hz uniformly while 1101 is ~126 Hz, so its data is tokenized/described at the wrong physical rate — either correct the per-session rate or exclude 1101.

### [MEDIUM] DATA-11: TRAIN dataset "kuhar" (KU-HAR) — converter, data/kuhar/{mani — provenance / reproducibility (converter vs committed manifest)
- [ ] **Issue:** data/kuhar/manifest.json does NOT match what convert.py::create_manifest generates. The on-disk model-facing channel descriptions were hand-edited to add '(waist)' and set 17 activities, but the converter still emits generic 'Accelerometer X-axis' (no placement) and 18 activities. Re-running the converter would REGRESS the channel text HALO conditions on (drop the placement token); the descriptions feeding the model are not reproducible from code.
  - **Evidence:** convert.py:255 says 90 subjects performing 18 activities, :259 description Accelerometer X-axis; committed manifest.json:3 says 17 activities, :9 Smartphone accelerometer X-axis (waist). Both convert.py:257 and manifest.json:5 say num_subjects 90 while only 89 distinct subjects exist on disk (config correctly says 89).
  - **Fix:** Make datascripts/kuhar/convert.py::create_manifest emit exactly what is committed so the model-facing text is regenerable: set num_subjects=89, change the description to "89 subjects performing 17 activities ...", and add the placement token to each channel description ("Smartphone accelerometer X-axis (waist)", ... "Smartphone gyroscope Z-axis (waist)"). Ideally source placement/num_subjects/activity-count programmatically (e.g. from dataset_config.json's placement:"waist"/num_subjects:89, and len(activity_counts) computed during conversion) rather than hardcoding, so future edits stay in sync. Also reconcile the ACTIVITIES mapping (table_tennis vs the on-disk playing_sports) if full byte-for-byte regeneration of the committed data is desired. After the change, re-running convert.py must reproduce the committed manifest.json byte-for-byte (or the pipeline should assert equality) so the descriptions HALO conditions on are provably code-derived.

### [MEDIUM] DATA-12: TRAIN dataset "hapt" (data/hapt, datascripts/hapt/convert.py — labels / corpus-composition (cross-dataset subject leakage + duplication)
- [ ] **Issue:** HAPT is the postural-transition superset of the UCI-HAR train dataset: identical 30 subjects, same waist-mounted Samsung Galaxy S II recordings at 50 Hz. Both are in the 11-dataset training corpus, and 2069/2546 HAPT sessions (81%) are the 6 basic activities (walking/upstairs/downstairs/sitting/standing/lying) that duplicate UCI-HAR content from the SAME subjects. The subject-disjoint split keys groups by (dataset, subject) at multi_dataset_loader.py:375, so ('hapt', k) and ('uci_har', k) are DISTINCT groups despite being the same physical person — they are shuffled independently across the global 70/15/15 partition, so a subject can sit in hapt-train and uci_har-val simultaneously. The intra-dataset subject-disjoint fix does NOT catch this hapt/uci_har duplicate pair, so those 30 shared subjects contaminate the (nominally subject-disjoint) model-selection val and the 6 basic classes are ~2x over-weighted.
  - **Evidence:** manifest descriptions identical ('30 subjects ... waist-mounted Samsung Galaxy S II ... 50Hz', same UCI source); hapt subjects={1..30}, uci_har subjects parsed via int(session_id.split('_')[1])={1..30} => shared={1..30} (30/30); split keying `key = (s['dataset'], subj)` at multi_dataset_loader.py:375; 2069/2546 (81%) hapt sessions in UCI-HAR's 6 basic classes.
  - **Fix:** Two independent fixes, both sound. (A) Leakage: in _create_splits, co-assign the hapt/uci_har pair by mapping both to one provenance-family key so subject k's two records always share a split. Minimal change at multi_dataset_loader.py:374-375, e.g. define FAMILY = {'hapt':'uci_har_family','uci_har':'uci_har_family'} and use fam = FAMILY.get(s['dataset'], s['dataset']); key = (fam, subj) if subj is not None else (fam, '__sess__'+str(session_id)). This makes the subject-disjoint val guarantee hold across the duplicate pair. (B) Over-weighting: de-duplicate the corpus by keeping HAPT transitions-only (drop its 2069 basic-class sessions) or dropping uci_har, so the same 30 subjects' 6 basic activities are not double-counted. Do both: (A) restores the subject-disjoint claim; (B) removes the ~2x waist/basic-activity bias. Severity is medium not high because the headline zero-shot metric is on 6 held-out test sets (neither hapt nor uci_har), so reported test numbers are not directly inflated; harm is confined to model-selection val integrity and corpus composition.

### [MEDIUM] DATA-13: TRAIN dataset "mhealth" — end-to-end audit (converter → data — preprocessing
- [ ] **Issue:** The 6 gyroscope channels (ankle_gyro_{x,y,z}, arm_gyro_{x,y,z}) are physically degenerate: they are near-unit-magnitude vectors (|gyro|≈1.0) whose magnitude is essentially INVARIANT to activity, so they carry no angular-velocity information — yet they pass the IMU filter and are trained as real gyro, and the channel text describes them as 'angular velocity'. This corrupts ~29% (6/21) of mhealth's IMU channels for the CLIP alignment signal and pollutes the model's cross-dataset notion of 'gyroscope'.
  - **Evidence:** Per-subject (all 10) ankle_gyro |mag|: sitting vs running ≈ equal (e.g. subj1 sit=1.131 run=1.036; subj5 sit=1.038 run=0.976; run/sit ratio 0.83–1.84 vs >>10 expected). Static postures nonzero: sitting=1.13, standing=1.04, lying=0.84 rad/s (a real gyro reads ~0 at rest). Over 300 sessions gyro|mag|.std mean=0.038 (flat) vs acc|mag|.std mean=2.70; corr(gyro_std,acc_std)=0.27. R²(gyro ~ linear(acc_unit))=0.008 so it is not even an orientation vector. Fed to training via datasets/imu_pretraining_dataset/multi_dataset_loader.py:419 IMU_PATTERNS=['acc','gyro','mag','ori']; labeled 'Left ankle angular velocity X-axis' at data/mhealth/manifest.json:46 / datascripts/mhealth/convert.py:199.
  - **Fix:** Re-fetch and re-convert mhealth from a clean MHEALTHDATASET and validate all derived channels before use: assert that gyro |mag| varies strongly with activity (running/jogging >> sitting/lying) and that magnetometer |mag| is roughly constant (~Earth field), not activity-scaled. The on-disk 'gyro' is a near-constant unit vector and the on-disk 'mag' behaves like a gyro, so the columns/derivation appear corrupted for the whole gyro+mag block, not just gyro. Until validated, exclude mhealth gyro (and mag) from the IMU set (e.g., drop them in the manifest/parquets or filter them out in multi_dataset_loader) and never describe a degenerate channel as 'angular velocity'. Add a converter sanity check comparing gyro/mag magnitude dynamics across static vs vigorous activities to catch this class of corruption for all datasets.

### [MEDIUM] DATA-14: TRAIN dataset "recgym" (data/recgym, datascripts/recgym/conv — augmentation
- [ ] **Issue:** Because each recgym acc axis is centered at ~0.5, the DC-vs-RMS gravity detector _gravity_present returns True for 100% of recgym sessions (dc/rms ~ 1.0), so the default_v2 gravity augmentation (p=0.5) high-pass-filters the normalized signal (removing <0.4 Hz content) and rewrites the channel description to '(gravity removed)' — a false statement, since the removed 'gravity' is actually the 0.5 min-max offset, not a physical gravity vector. This directly pollutes the CLIP text-alignment target. The yaw_rotation augmentation (p=0.5) estimates the 'down' axis as the mean of the acc triad, which for recgym is a bogus diagonal ~[0.58,0.58,0.58] (the normalization centroid, not gravity), then rotates the co-located acc+gyro triads about that fake axis — a physically meaningless SO(2) mixing of normalized channels.
  - **Evidence:** Reproduced on 10 recgym sessions: _gravity_present=True for all (e.g. s44_squat_0007 dc=0.865 rms=0.866; s574_leg_press_0000 dc=0.866 rms=0.866), yaw 'down' axis ~[0.58,0.57,0.58] every session. augmentations.py:812-825 (_gravity_present), :960-986 (_gravity marks '(gravity removed)'), :989-1026 (_yaw_rotation axis from acc mean), :757-767 (default_v2 enables gravity+yaw).
  - **Fix:** Gate gravity/yaw on an absolute physical-units gravity check (acc DC magnitude ≈1 g / 9.8 m/s^2 and axis-concentrated, not diagonal) instead of the unit-agnostic dc>0.3*rms; or carry a per-dataset normalized flag and opt recgym out of physics augmentation; ideally fix the recgym-normalization root cause (finding-1) so acc carries true gravity.

### [MEDIUM] DATA-15: capture24 (TRAIN dataset) — converter, data, config, loader  — augmentation/labels
- [ ] **Issue:** capture24 is the only one of the 11 TRAIN datasets with no entry in DATASET_CONFIGS, so augment_label() falls to the generic fallback: no dataset-specific synonyms, only a raw label or one of 3 generic templates at 50% probability. Worse, its 3 underscored multiword labels (household_chores, manual_work, mixed_activity) are never normalized to natural phrases and reach the frozen SentenceBERT text encoder with underscores intact, unlike configured datasets where synonyms convert e.g. 'walking_upstairs'->'walking upstairs'. This gives capture24 (the largest free-living train component) near-zero label-text diversity for CLIP alignment and corrupted text for ~29% of its sessions.
  - **Evidence:** datasets/imu_pretraining_dataset/label_augmentation.py DATASET_CONFIGS (lines 231-420) has no 'capture24' key; fallback at lines 458-462. Empirical: augment_label('household_chores','capture24',rate=1.0) over 200 draws -> {'household_chores','human household_chores','person household_chores','household_chores activity'} (underscore preserved); compare augment_label('cycling','pamap2') -> dozens of natural variants ('riding bicycle','physical activity: biking',...). Underscored-label sessions: household_chores 7162 + mixed_activity 5672 + manual_work 1050 = 13,884/48,518 (29%).
  - **Fix:** Add a 'capture24' block to DATASET_CONFIGS mirroring the other train datasets: map every capture24 label to natural-language synonyms, especially the underscored ones (household_chores -> ['household chores','doing chores','housework']; manual_work -> ['manual work','manual labor','physical labor']; mixed_activity -> ['mixed activity','varied activity','mixed movement']; vehicle -> ['riding in a vehicle','in a car','traveling by vehicle']; bicycling -> ['bicycling','cycling','riding a bike']; sports -> ['playing sports','sports activity']; plus sitting/standing/walking/sleeping), and a templates list matching the other configs. This gives capture24 real CLIP-text diversity and de-underscores its labels the same way synonyms already do for e.g. walking_upstairs->'walking upstairs'. Additionally (defense-in-depth for any future unconfigured dataset), normalize underscores in the training text path — e.g. apply label.replace('_',' ') to the base label before/inside augment_label's fallback — to match the eval-time normalization at eval_v2.py:415 and generate_eval_v2_labels.py:67, so no unconfigured train dataset can ever leak underscored text to the frozen SentenceBERT encoder.

### [LOW] DATA-16: TRAIN dataset "uci_har" — end-to-end audit (converter → data — augmentation / channel-desc
- [ ] **Issue:** The default_v2 gravity augmentation (p=0.5) removes gravity from total_acc (it still carries gravity) while leaving body_acc unchanged (already gravity-removed and skipped by _gravity_present). The result is TWO near-identical accelerometer triads whose descriptions both become '...acceleration ...(gravity removed)'. So ~half of all uci_har training samples present the channel-independent encoder with two duplicate channels + duplicate text, which dilutes/duplicates the accel contribution and gives the CLIP text head two collinear channel embeddings.
  - **Evidence:** augmentations.py _gravity (lines 960-986) + _gravity_present (812-825): total_acc DC~1g passes, body_acc DC~0 is skipped; _mark_gravity_removed rewrites the total_acc text. Forced-gravity harness on 40 random sessions: mean per-axis cosine(body_acc, gravity-removed total_acc) = 0.955, 90% of sessions >0.9, relative RMS(total-body)/RMS(body)=0.28. Live loader (default_v2) sample dump: body_acc_y desc 'Body acceleration Y-axis ...(gravity removed)' alongside total_acc_y 'Total acceleration Y-axis ...(gravity removed)' — two near-duplicate channels with near-duplicate text in the same sample.
  - **Fix:** Root cause is the dual-acc layout (drop the redundant total_acc triad for uci_har in the converter/loader so only one acc representation is presented). If total_acc is retained, make the gravity aug layout-aware: skip total_acc when a body_acc (gravity-removed) triad already coexists in the same sample, or drop total_acc when gravity fires — so the encoder never simultaneously receives body_acc and a gravity-removed total_acc with identical channel text. Severity is low: it affects only uci_har (1 of 11 train datasets) at p=0.5, marginally worsens an already-redundant channel layout, and causes no leakage, eval corruption, or crash — purely a modest training-signal-dilution issue downstream of the separately-tracked dual-acc redundancy finding.

### [LOW] DATA-17: TRAIN dataset "hhar" (Heterogeneity Activity Recognition) —  — channel-desc
- [ ] **Issue:** hhar accelerometer is in m/s^2 WITH gravity (per-session accel-magnitude mean ~10.04, corpus p5-p95 = 9.48-10.45), but neither the manifest dataset description nor the per-channel descriptions state the units or that gravity is included. Other train sets differ in convention (e.g. uci_har body_acc is in g and gravity-removed), so the model's frozen-SBERT channel-text conditioning receives identical 'Accelerometer X-axis' text for signals that differ by ~10x in scale and by gravity presence. The default_v2 gravity augmentation relies on DETECTING gravity (_gravity_present) rather than reading it from text, and marks '(gravity removed)' only after acting — the base description never encodes the default state, so unaugmented samples carry no unit/gravity signal.
  - **Evidence:** data/hhar/manifest.json channels[*].description = 'Accelerometer X-axis' (no units, no gravity note); dataset description mentions rate/placement but not units/gravity. Measured accel-magnitude mean 10.043 over 300 random sessions (m/s^2 + gravity). Compare augmentations.py:812-838 (_gravity_present / _mark_gravity_removed) which infer state instead of trusting text.
  - **Fix:** Adopt the proposed fix but broaden it corpus-wide rather than hhar-only, verifying each manifest against the measured scale so the frozen-SBERT channel-text conditioning is explicit and internally consistent:

- data/hhar/manifest.json: acc_{x,y,z} → "Accelerometer X-axis (m/s^2, includes gravity)" (measured mag ~9.9-10.1). Optionally add units/gravity note to the dataset `description` too.
- data/dsads/manifest.json: acc_* → append "(m/s^2, includes gravity)" (measured ~10.2).
- data/hapt/manifest.json: acc_* → append "(g, includes gravity)" (measured ~1.03).
- data/kuhar, unimib_shar, recgym, wisdm, mhealth: measure the per-triad magnitude, infer units (g vs m/s^2) and gravity state, and append the same "(<units>, includes gravity | gravity removed)" clause — matching the phrasing already used by uci_har/pamap2/capture24.

Keep the augmentation contract intact: _mark_gravity_removed already strips a contradictory "includes gravity" clause (augmentations.py:828-838), so a base "(m/s^2, includes gravity)" will be correctly rewritten to "(m/s^2) (gravity removed)" when the P1 gravity aug fires. This makes the base (unaugmented / eval-time) state carry the unit+gravity signal instead of relying on the detector, and removes the ~10x-scale text ambiguity across the corpus.

### [LOW] DATA-18: pamap2 TRAIN dataset — end-to-end (datascripts/pamap2/conver — sampling / reproducibility / data-hygiene
- [ ] **Issue:** 145 orphan session directories exist on disk that are not in labels.json (4522 parquet dirs vs 4377 labels.json keys, matching config num_sessions=4377). Windowing is non-deterministic (no seed) and the converter never clears sessions/ before writing, so a prior run's extra windows persist and the exact training windows are not reproducible.
  - **Evidence:** On-disk session dirs = 4522, labels.json keys = 4377, missing files = 0, orphans = 145 (subjects 101-108, all high window-index tails e.g. subject101_seg001_0070). windowing.py:277-279 seeds only if seed is not None; convert.py:145-150 calls create_variable_windows WITHOUT seed and main() never seeds random before conversion; convert.py:154-158 writes into OUTPUT_DIR/sessions without clearing it. Loader builds its index from labels.json keys, so orphans are inert but signal a dirty, non-reproducible data dir (pamap2 analogue of the known hhar 318k-vs-146k discrepancy).
  - **Fix:** Two-part fix, both valid: (1) at the start of convert.py main(), remove/clear OUTPUT_DIR/sessions (e.g. shutil.rmtree(OUTPUT_DIR/'sessions', ignore_errors=True)) before writing so stale windows from prior runs can't accumulate; (2) make windowing deterministic. Prefer a single random.seed(FIXED_SEED) at the top of main() rather than passing a fixed seed into every create_variable_windows call — a per-call constant seed re-seeds identically for each segment and collapses window-length variety across segments. Either restores exact on-disk==labels.json reproducibility. Impact is low: orphans never reach training (loader indexes labels.json keys) and the committed parquet files pin the existing checkpoint's data, so reported results are unaffected; this is a data-hygiene / regenerate-from-source reproducibility fix, not a results-correctness bug.

### [LOW] DATA-19: TRAIN dataset "wisdm" end-to-end (data/wisdm + datascripts/w — plumbing
- [ ] **Issue:** get_channel_counts() reports the MANIFEST channel count (12) for every WISDM session, but each session parquet holds only one sensor's 3 channels. The ChannelBucketBatchSampler therefore files all ~164k WISDM samples into a phantom '12-channel' bucket (its own docstring says '12: wisdm'), separate from the genuine 3-channel bucket (unimib_shar), and prints misleading per-bucket stats. It is only benign today because MAX_BS_PER_BUCKET={} makes every bucket use the default batch size; if per-bucket batch sizes are re-enabled (the entire purpose of the sampler) WISDM's 3-channel data would get the 12-channel micro-batch size, and bucket_probs/num_batches are already computed on the wrong counts. This is distinct from the known 'single-sensor data' issue — it is the sampler metadata never reconciled with it.
  - **Evidence:** multi_dataset_loader.py:747-754 get_channel_counts uses `self.dataset_info[dataset_name]['channels']` (manifest, 12 for wisdm) while __getitem__:420-453 selects only the 3 channels present in the parquet. semantic_alignment_train.py:57 docstring '12: wisdm', :285 `MAX_BS_PER_BUCKET = {}`, :2258-2266 pass channel_counts into ChannelBucketBatchSampler.
  - **Fix:** In multi_dataset_loader.get_channel_counts() (lines 747-754), stop deriving the count from the manifest. Instead derive each session's IMU-channel count from its actual data — e.g., cache the per-session channel set during the initial index/label scan (self.sessions already stores path/dataset/label; add a 'num_imu_channels' computed from the parquet columns using the same IMU_PATTERNS filter as __getitem__), or scan parquet schemas once and memoize. Then get_channel_counts returns 3 for WISDM sessions, merging them into the genuine 3ch bucket with unimib_shar. Also update/remove the stale "12: wisdm" mapping in the ChannelBucketBatchSampler docstring (semantic_alignment_train.py:57). This makes bucketing, per-bucket log stats, and any future per-bucket batch sizes correct, and closes the latent footgun without changing today's (uniform-BS) behavior.

### [LOW] DATA-20: dsads (TRAIN dataset) — converter, manifest/labels/config, l — augmentation x channel-description
- [ ] **Issue:** Rate augmentation yields self-contradictory channel text for DSADS. The manifest description ('...triaxial accelerometer, gyroscope, and magnetometer at 25Hz.') is prepended verbatim to every channel description; default_v2 RateCfg (p=0.5) resamples and updates sample.sampling_rate, then the loader appends '(sampled at {N}Hz, {w}s window)'. When rate-aug fires the text becomes e.g. '... at 25Hz ... (sampled at 63Hz, 2.0s window)' -- the load-bearing rate token contradicts the embedded literal.
  - **Evidence:** data/dsads/manifest.json description embeds '25Hz'; augmentations.py RateCfg.default_v2 enabled (p=0.5) with _rate() setting s.sampling_rate; multi_dataset_loader.py:586-590 appends the 'sampled at {sampling_rate:.0f}Hz' suffix while base_channel_descriptions still contains the prepended '...at 25Hz'. 9/11 train manifests embed a Hz literal (uci_har/hhar/wisdm/unimib_shar/dsads/hapt/kuhar/recgym/capture24).
  - **Fix:** Strip the Hz literal from the 9 affected manifest descriptions (uci_har, hhar, wisdm, unimib_shar, dsads, hapt, kuhar, recgym, capture24) so the augmentation-updated "(sampled at {N}Hz, {w}s window)" suffix is the sole, self-consistent rate token in the non-filterbank/headline path. Equivalent alternatives: stop prepending the full dataset_desc into per-channel text (multi_dataset_loader.py:490-496), or have RateCfg._rate() rewrite/remove any embedded rate token when it mutates sampling_rate. Note the filterbank redesign already drops the suffix entirely, so the literal must still be stripped there (otherwise the stale "25Hz" persists with resampled data), but no suffix contradiction arises in that path.

### [LOW] DATA-21: TRAIN dataset "kuhar" (KU-HAR) — converter, data/kuhar/{mani — augmentation (default_v2 P1/P2 interaction)
- [ ] **Issue:** Because kuhar acc is gravity-removed AND many postures are low-motion, the _gravity_present DC/RMS guard (dc > 0.3*rms) MISFIRES as True on 21.7% of sessions, precisely the static postures. On those sessions default-ON default_v2 augs do the exact thing the guard was written to prevent: P1 _gravity high-passes already-gravity-free data and stamps an inconsistent '(gravity removed)' onto the channel text (identical gravity-free data then gets two different texts), and P2 _yaw_rotation rotates the acc/gyro triads by up to +/-180 deg about a down axis estimated from a ~0.02 m/s^2 pure-noise DC vector.
  - **Evidence:** augmentations.py:825 return dc > 0.3 * rms, docstring says it Prevents yaw-rotation from spinning about a garbage near-zero gravity axis. Empirical: _gravity_present True on 65/300 (sitting 22/29=76%, lying 13/31=42%, standing 14/35=40%, talking_sitting 50%). default_v2 enables gravity+yaw_rotation (augmentations.py:759-762) and is the training default (semantic_alignment_train.py:382).
  - **Fix:** Adopt the finding's proposed fix: make `_gravity_present` require an absolute DC magnitude in addition to the ratio, e.g. `return dc > 0.3*rms and dc > ABS_G_MIN` where ABS_G_MIN is unit-aware (~2 m/s^2, or ~0.2 in g units). Since the guard is unit-agnostic today, either normalize/branch on inferred units or key the absolute threshold off the acc RMS scale. This makes the guard return False for gravity-removed data (KU-HAR, uci_har body_acc, iOS userAcceleration) as its docstring already promises, so P1/P2 correctly skip those triads. Because impact is low, this is a robustness/consistency cleanup rather than a results-affecting bug — a lighter alternative is a per-dataset `gravity_removed` flag in the manifest/dataset_config that skips P1/P2 and leaves the channel text as-is.

### [LOW] DATA-22: unimib_shar (TRAIN dataset) — converter, config, loader, pat — channel-desc / augmentation / sampling-rate
- [ ] **Issue:** The manifest DESCRIPTION embeds a literal 'at 50Hz' that the loader prepends to every channel description, on top of the appended '(sampled at NHz, Xs window)' suffix (small_deep uses feature_extractor_type=spectral_temporal, so the legacy suffix path is active). Without augmentation the text redundantly says '...at 50Hz ... (sampled at 50Hz ...)'; with default_v2 rate augmentation (RateCfg p=0.5, resamples 15-100Hz) the appended suffix reflects the new rate but the embedded 'at 50Hz' does NOT update, so ~50% of unimib train samples carry a channel description that states two different sampling rates.
  - **Evidence:** manifest description '...(9 ADL + 8 falls) at 50Hz. NOTE:...'; multi_dataset_loader.py L491-492 prepends dataset_desc to each channel desc, L613-617 appends '(sampled at {sampling_rate:.0f}Hz, {patch_size_sec:.1f}s window)' using the (aug-updated) rate. augmentations.py RateCfg default_v2 enabled p=0.5. model/config small_deep: feature_extractor_type=spectral_temporal, dft_size=None (legacy suffix path, not filterbank).
  - **Fix:** Remove the hard-coded rate token from the dataset descriptions so the loader-generated suffix (which co-varies with rate augmentation) is the only rate signal. Two equivalent options: (a) one-time normalize the manifests — strip the "\d+\s*Hz" phrase from the "description" field of every data/<ds>/manifest.json (9 train + several eval manifests embed one; unimib_shar: change "...(9 ADL + 8 falls) at 50Hz. NOTE:..." to "...(9 ADL + 8 falls). NOTE:..."), and update the converters in datascripts/<ds>/convert.py that emit those strings so regeneration stays clean; or (b) strip it at load time — in multi_dataset_loader.py near L476/L491, run dataset_desc through a regex like re.sub(r"\s*(?:sampled\s+)?at\s+\d+(?:\.\d+)?\s*Hz", "", dataset_desc, flags=re.I) (or a broader r"\b\d+(?:\.\d+)?\s*Hz\b" strip) before prepending, so the embedded native-rate never reaches the text. Option (a) is preferable because it fixes the data corpus-wide and keeps the filterbank path clean too.

### [LOW] DATA-23: TRAIN dataset "mhealth" — end-to-end audit (converter → data — sampling-rate
- [ ] **Issue:** The gyro channels are upsampled to the declared 50 Hz by a clean ~3.5x sample-and-hold: only ~28% of gyro rows are unique, so the true gyro content is ~14 Hz while manifest.json declares sampling_rate_hz=50 for every channel. The physical-Hz filterbank tokenizer places its bands (F_MAX=15 Hz) assuming 50 Hz, so it treats the held/aliased gyro as genuine 15-Hz-bandwidth signal.
  - **Evidence:** dup-fraction (row == previous row) for ankle_gyro/arm_gyro = 0.715–0.725 for every sampled session across subjects 1/5/7/8/10; hold run-length mean=3.49, mode=3; distinct_rows≈n/3.6. acc/ecg/mag dup-fraction ≈ 0.000. Raw rows show blocks like [0.5974,-0.7411,-0.3811]×3 then [0.5176,-0.7861,-0.4244]×3. Manifest declares 50 Hz uniformly (datascripts/mhealth/convert.py:199-201, 109).
  - **Fix:** Do not advertise 50 Hz for the gyro channels. Concretely: in datascripts/mhealth/convert.py, detect the ~3.5x sample-and-hold and either (a) set the six gyro channels' per-channel manifest sampling_rate_hz to the true ~14 Hz (the manifest schema already supports per-channel rates, and multi_dataset_loader.py:467/556/566 would then pass the correct rate to the physical-Hz filterbank, keeping bands within the true Nyquist and correcting the "sampled at NHz" channel text), or (b) drop the degenerate gyro channels entirely (they are already droppable via channel_dropout p=0.3). Simply keeping them at a declared 50 Hz is wrong because the filterbank's 7–15 Hz bands then tokenize sample-and-hold aliases as genuine signal. Impact is bounded — mhealth is ~2.4% of effective training windows and only 6 of 21 IMU channels — so this is a data-integrity/methodological correctness fix rather than a results-moving bug.

### [LOW] DATA-24: TRAIN dataset "recgym" (data/recgym, datascripts/recgym/conv — preprocessing/sampling-rate
- [ ] **Issue:** convert.py groups rows only by (subject, workout) and ignores the raw 'Session' column, so all recording sessions of a given (subject, activity) — the dataset's 5 sessions/subject — are concatenated into ONE synthetic block, then windowed. The timestamp is fabricated as np.linspace(0,(n-1)/20,n), i.e. exactly 20 Hz by construction. Consequences: (1) variable-length windows can straddle real recording-session boundaries, injecting discontinuity jumps mid-window; (2) the synthetic timestamp makes any sampling-rate self-check vacuous — timestamp_sec cannot validate the true inter-sample timing.
  - **Evidence:** Exactly 110 (subject,activity) blocks on disk = 11 activities x 10 subjects (Session dimension collapsed), each block spanning many windows (mean 64, up to 138 e.g. s551_running_0138), implying multi-session merge. convert.py:179-187 (loops only subject_col x workout_col), :204 (timestamp_sec=np.linspace(...SAMPLE_RATE...)). Measured rate=20.00 on every sampled session is an artifact of the linspace, not a real measurement.
  - **Fix:** In datascripts/recgym/convert.py, detect the 'Session' column the same way as the others and add it as a third grouping key inside the subject×workout loop (lines 179-187), so each physical (subject, workout, session) recording becomes its own block fed to create_variable_windows — e.g. session_id = f"s{subject_id:02d}_{activity_name}_sess{int(session)}". This guarantees no window spans an across-day boundary. Recompute the np.linspace timestamp per-session (so it resets to 0 at each real recording). If the raw CSV exposes any true per-sample time/index, propagate it into timestamp_sec instead of linspace so the 20 Hz assumption becomes auditable; otherwise a per-session linspace is at least boundary-consistent. Then re-run the converter to regenerate data/recgym. Because timestamp_sec is not consumed by the loader and the manifest rate (20 Hz, correct) is what training uses, the only behavioral change is cleaner, boundary-free training windows for recgym; no eval or fairness path is affected.

### [LOW] DATA-25: TRAIN dataset "recgym" (data/recgym, datascripts/recgym/conv — plumbing/reproducibility
- [ ] **Issue:** Subject (hence session) IDs are built from hash(str(subject)) % 1000. Python string hashing is salted by PYTHONHASHSEED (randomized by default), so re-running convert.py produces DIFFERENT subject IDs, silently invalidating dataset_config's recgym train_subjects/test_subjects and any downstream that keys on them. The intended reproducible branch int(subject) is dead code: pandas loads the subject column as numpy.int64, and isinstance(np.int64(x),(int,float)) is False, so the fragile hash branch is always taken.
  - **Evidence:** convert.py:196 subject_id = int(subject) if isinstance(subject,(int,float)) else hash(str(subject))%1000. On-disk subject prefixes are hash-like {s44,s93,s334,s427,s547,s551,s574,s806,s940,s978} — not the s01..s10 the int branch would yield — proving the hash path executed. dataset_config train/test_subjects list exactly these hash values.
  - **Fix:** In datascripts/recgym/convert.py drop the fragile isinstance guard and make subject IDs deterministic: `try: subject_id = int(subject)  # int() accepts np.int64 -> s01..s10, reproducible; except (ValueError, TypeError): subject_id = int(hashlib.md5(str(subject).encode()).hexdigest(), 16) % 1000` (stable, non-salted). Apply the same non-salted hashlib to line 220's `seed=42 + hash(session_id)%1000` so window boundaries are reproducible too. After regenerating, either update dataset_config recgym train_subjects/test_subjects to the new deterministic values (s01..s10) or, better, stop hardcoding them since the subject-disjoint split derives subjects from on-disk prefixes via _subject_of and does not read these fields. Optional hardening: %1000 gives ~4.5% birthday-collision risk over 10 subjects on regeneration (two subjects could silently merge into one group -> subject leakage); the deterministic int() path removes this for integer subject columns. Alternatively set PYTHONHASHSEED in the converter entrypoint if hashing must stay.

### [LOW] DATA-26: capture24 (TRAIN dataset) — converter, data, config, loader  — channel-desc/sampling-rate
- [ ] **Issue:** The manifest/dataset description hard-codes the literal 'at 100 Hz', and the loader prepends this whole description to every channel description fed to the frozen text encoder. The filterbank path deliberately omits the Hz suffix (so rate stays out of the semantic text), and the P3 rate augmentation resamples the signal to a random 15-100 Hz and updates sample.sampling_rate so downstream conditioning co-varies. But the embedded 'at 100 Hz' literal does NOT co-vary: it contradicts the resampled signal ~half the time and re-injects a fixed rate token into text the design intends to keep purely semantic, partially defeating the rate-invariance augmentation. (Systemic across datasets with embedded rates, but capture24 exhibits it.)
  - **Evidence:** data/capture24/manifest.json description: '...Axivity AX3 on the dominant wrist at 100 Hz...'; multi_dataset_loader.py L490-496 prepends dataset_desc to ch_desc; L578-593 filterbank branch comment 'the channel text stays purely semantic - no Hz/window suffix'; augmentations.py RateCfg L642-651 (min_hz=15,max_hz=100,p=0.5). Live run of IMUAugmenter.default_v2() on a capture24 sample produced sampling_rate=61.8 Hz while the channel description still asserted 'at 100 Hz'.
  - **Fix:** Implement the second (embedded-literal) half of research_conditioning §7 decision #1: strip the "at N Hz" phrase from every dataset manifest description (rate belongs to the tokenizer/metadata + the loader's co-varying suffix, not the semantic prose). This is a one-edit-per-manifest change across the ~9 datasets that embed rates (capture24, hhar, kuhar, dsads, recgym, uci_har, hapt, unimib_shar, wisdm, ...). Alternatively/additionally, add a load-time sanitizer or an augmenter step that regex-removes any "N Hz"/"NHz" substring from dataset_desc the way _mark_gravity_removed rewrites the gravity clause, so the embedded rate is removed (filterbank) or co-varies (legacy suffix already carries the achieved rate). Prefer stripping from the manifests since it is the cleanest and is exactly what §7 mandates; it also removes the per-dataset rate fingerprint before the M4 filterbank cutover where the text must be purely semantic.


## MODELS (23)

### [HIGH] MODELS-1: HALO v2 evaluation clarity/meaningfulness: docs/baselines/RE — reproducibility/plumbing
- [ ] **Issue:** The published RESULTS_V2 table cannot be regenerated from committed artifacts, and the documented assemble command produces a completely different table. Running `assemble_v2_table.py` today yields HALO avg 32.9 (inclusivehar em-dash, mobiact 12.8, harth 13.2, no opportunity), CrossHAR avg 22.1 from a SINGLE dataset (shoaib only), LiMU-BERT avg 12.8 from a single dataset (harth only) — nothing like the doc's full 6-dataset CrossHAR 27.1 / LiMU-BERT 18.3 rows. Even the cells that DO have committed JSONs contradict the doc: LiMU-BERT harth is 12.76 in baseline_v2_limubert.json but 2.4 in the doc; opportunity 7.38 in JSON vs 8.3 in doc. The doc's full baseline rows have no traceable source JSON.
  - **Evidence:** Ran `python3 assemble_v2_table.py` -> CrossHAR row `— — — 22.1 — — | **22.1**`, LiMU-BERT `— — — — 12.8 — | **12.8**`. baseline_v2_crosshar.json contains only `shoaib` (22.06); baseline_v2_limubert.json contains only opportunity(7.38)+harth(12.76). RESULTS_V2.md:58 `LiMU-BERT ... 8.3 | 2.4 | 18.3` disagrees with both committed cells.
  - **Fix:** Make doc == artifact for the central comparison table. Concretely: (a) Decide the benchmark composition once — the current held-out set is [motionsense, realworld, mobiact, shoaib, harth, inclusivehar] (opportunity demoted), so update RESULTS_V2.md to that set and align assemble_v2_table.py:7 (they already use inclusivehar; the doc still uses opportunity). (b) Re-run run_baselines_v2.py for CrossHAR and LiMU-BERT over ALL benchmark datasets and commit complete per-model JSONs to test_output/eval_v2/baseline_v2_{crosshar,limubert}.json (today they hold only shoaib, and opportunity+harth respectively). (c) Merge the stray HALO per-dataset JSONs (e.g. tsfm_v2_inclusivehar.json, tsfm_v2_mobiact_fkl_fixed.json) into the native/parity JSONs the assemble script reads, so no cell is silently em-dashed. (d) Regenerate the table with assemble_v2_table.py and paste its output verbatim into RESULTS_V2.md, replacing the current unbacked rows (CrossHAR 30.3/27.3/13.6/22.1/48.5/20.8; LiMU-BERT 43.0/20.5/8.0/27.6/8.3/2.4) — in particular the LiMU-BERT harth 2.4 that the committed JSON says is 12.76. (e) Until a model has all datasets, mark absent cells N/A and do not print an avg over a partial set (the script currently averages over whatever cells exist, e.g. CrossHAR avg 22.1 from one dataset — misleading). Add a regeneration check (e.g. a test or make target) that diffs the assemble output against the committed table so the doc can never drift from the artifacts again.

### [MEDIUM] MODELS-2: BUILT baseline "limubert" — val_scripts/human_activity_recog — eval-clarity
- [ ] **Issue:** The reported LiMU-BERT results row is not reproducible from the current adapter+driver: the published numbers, the on-disk v2 JSON, and the driver's dataset list all disagree.
  - **Evidence:** docs/baselines/RESULTS_V2.md:58 lists LiMU-BERT over 6 datasets incl. opportunity (motionsense 43.0, opportunity 8.3, harth 2.4, avg 18.3), but test_output/eval_v2/baseline_v2_limubert.json contains only keys ['_baseline','_tier','opportunity','harth'] with DIFFERENT values (opportunity f1=7.38, harth f1=12.76) — 2/6 datasets and non-matching. Meanwhile run_baselines_v2.py:30 DATASETS=[...,'harth','inclusivehar'] (opportunity replaced by inclusivehar), so re-running the adapter produces an inclusivehar row that appears nowhere in RESULTS_V2.md.
  - **Fix:** Regenerate the baseline rows from a clean run rather than hand-patching. Concretely: (1) Re-run `python val_scripts/human_activity_recognition/run_baselines_v2.py --baselines limubert crosshar` over the canonical held-out test set — which is the inclusivehar-based list {motionsense, realworld, mobiact, shoaib, harth, inclusivehar} that both run_baselines_v2.py:30 and assemble_v2_table.py:7 already target, NOT the opportunity list. The current JSONs are partial (limubert 2/6, crosshar 1/6), so overwrite them with complete 6-dataset runs. (2) Regenerate the RESULTS_V2.md ZS-XD baseline table by piping `assemble_v2_table.py` output, so the published row is byte-derived from the JSON. (3) Reconcile the opportunity→inclusivehar transition doc-wide: RESULTS_V2.md still lists opportunity in the header/HALO/CrossHAR/LiMU-BERT rows and never mentions inclusivehar, while the code has fully switched — update the doc's benchmark-composition paragraph (lines 9-10) and every 6-dataset row (HALO native/parity, CrossHAR, LiMU-BERT) to the inclusivehar set, or explicitly document opportunity as retained if that is the intended benchmark. The fix's phrase "canonical 6-dataset list" must be disambiguated to the inclusivehar list to match the code, not the opportunity list in the current doc.

### [MEDIUM] MODELS-3: CrossHAR baseline (val_scripts/human_activity_recognition/ba — paper-fidelity
- [ ] **Issue:** CrossHAR's pretraining corpus is stale and does NOT match HALO's 11-dataset training corpus: it is missing capture24 (the largest free-living training set). The pretraining checkpoint and the cached zero-shot classifier were built in February, before capture24 existed in LiMU-BERT format, so CrossHAR was pretrained + classifier-fit on only 10 of the 11 datasets HALO trains on. This breaks the 'same corpus' parity claim and handicaps the baseline (strictly less pretraining data than the proposed method).
  - **Evidence:** evaluate_crosshar.py:50 points at saved/pretrain_base_combined_train_20_120/model_masked_6_1.pt; create_combined_dataset.py:80 pools `for ds in TRAIN_DATASETS` (dataset_config train_datasets now = 11 incl capture24). Timeline: model_masked_6_1.pt mtime 2026-02-15, crosshar_zs_transformer.pt mtime 2026-02-17, but benchmark_data/processed/limubert/capture24/data_20_120.npy mtime 2026-07-09 — capture24 did not exist when the checkpoint/classifier were built. The auxiliary_repos/CrossHAR/dataset/combined_train dir is now absent (deleted), so the stale corpus cannot even be re-inspected.
  - **Fix:** Rebuild the CrossHAR pretraining corpus and artifacts from the current 11-dataset train_datasets (including capture24, applying the intended capture24 cap so it reconciles with HALO — dataset_config subsampling sets capture24 target_sessions=20000): (1) delete/regenerate auxiliary_repos/CrossHAR/dataset/combined_train via create_combined_dataset.py (fix the '10 training datasets' docstring); (2) re-run CrossHAR hierarchical pretraining to overwrite saved/pretrain_base_combined_train_20_120/model_masked_6_1.pt (and model_Contrastive_6_1.pt); (3) DELETE the cached test_output/baseline_evaluation/crosshar_zs_transformer.pt so evaluate_crosshar.py re-fits it on refreshed embeddings (otherwise the stale Feb-17 cache is silently reloaded at lines 881-885); (4) re-run evaluate_crosshar.py and update the CrossHAR row in RESULTS_V2.md, documenting the exact baseline corpus (11 datasets) and any capture24 cap. Additionally check the sibling LiMU-BERT baseline: all its saved/*.pt are Feb-dated, so it likely has the identical staleness (its combined_train checkpoint was 'repointed' this session but not shown to be rebuilt with capture24) — rebuild/re-eval it the same way for corpus parity.

### [MEDIUM] MODELS-4: CrossHAR baseline (val_scripts/human_activity_recognition/ba — eval-clarity
- [ ] **Issue:** A second, still-runnable scoring entry point (evaluate_crosshar.py __main__) produces CrossHAR numbers that are NOT the v2 protocol: it uses group-matched scoring (score_with_groups) instead of the ConSE bridge, adds a 1%/10% END-TO-END fine-tune tier (encoder unfrozen — an undocumented advantage over the frozen-encoder v2 path and over HALO's frozen zero-shot), and derives test ground truth from the v1 offset-subtracting get_window_labels rather than eval_v2.window_ground_truth. Writing these to test_output/baseline_evaluation/crosshar_evaluation.json makes them an attractive nuisance that could be cited as CrossHAR's result.
  - **Evidence:** evaluate_crosshar.py:1-8 docstring advertises 'group scoring' and 'end-to-end fine-tuning'; :927 test_labels = get_window_labels(raw_labels); :939/:948 score_with_groups(...); :957-968 '1%/10% Supervised (End-to-End Fine-Tuning)' via score_supervised_finetune (crosshar.py:654-657 deep-copies the encoder and sets requires_grad_(True)). The v2 adapter (crosshar.py:25-35) correctly does none of this — only frozen embeddings -> cached head -> softmax, bridged by run_baselines_v2.conse_predict.
  - **Fix:** Gate the legacy scoring so it can no longer emit CrossHAR numbers, while keeping the definitions the v2 adapter imports (load_crosshar_model, MaskedModel4Pretrain/TransformerClassifier, EMB_DIM, CROSSHAR_CHECKPOINT, extract_crosshar_embeddings). Concretely: (a) delete or hard-disable main() and the results writer (or move the scoring half to an archive/ path), leaving crosshar.py's imports intact; (b) delete/relocate the stale test_output/baseline_evaluation/crosshar_evaluation.json (and the analogous limubert/moment/lanhar/llasa JSONs) so v1 group-scored + subject-leaking fine-tune numbers cannot be cited; (c) update README.md:170 and docs/baselines/BASELINES_SETUP.md:140,148 (and ARCHITECTURE.md:731) to point CrossHAR evaluation at `run_baselines_v2.py --baselines crosshar` with results under test_output/eval_v2/. After this, the only producible CrossHAR numbers come from run_baselines_v2 (ConSE, frozen encoder, eval_v2.window_ground_truth).

### [MEDIUM] MODELS-5: UniMTS (planned "cosine-tier" baseline, released HuggingFace — channel-desc / paper-fidelity
- [ ] **Issue:** The released UniMTS checkpoint is ACCELEROMETER-ONLY (3 input channels), but every HALO doc describes it as a 6-axis acc+gyro model. baseline_flexibility.md matrix cell says 'fixed acc+gyro'; its prose says 'Expects 6-axis (acc+gyro) per placed sensor'; citation.json says 'consumes both accel AND gyro'. In the actual code base_channel=3 and is only doubled when --gyro is set, which defaults to 0 and is NOT set by the official reproduction script — so the single released checkpoint (checkpoint/UniMTS.pth) loads with in_channels=3 and the gyro half of every joint block is sliced away before the encoder. Setting --gyro 1 would make load_state_dict shape-mismatch (no 6ch checkpoint is released).
  - **Evidence:** contrastive.py:17-20 'base_channel = 3; base_channel = base_channel*2 if args.gyro'; evaluate.py:107 & evaluate_custom.py:109 "add_argument('--gyro', default=0)"; evaluate.py:61-64 'if not args.gyro: indices = [i..i+3 for i in range(0,c,6)]; input = input[:,:,indices]'; run_evaluation.sh passes neither --gyro nor --stft. Contradicts docs/baselines/baseline_flexibility.md:28 ('fixed acc+gyro') and :51 ('Expects 6-axis (acc+gyro) per placed sensor') and references/baselines/unimts/citation.json notes.
  - **Fix:** Correct all four locations to reflect that the released UniMTS checkpoint is accelerometer-only (3 channels per placed joint), as it is both pretrained (run_pretrain.sh, no --gyro) and evaluated (run_evaluation.sh, no --gyro) accel-only, and no 6-channel weights exist on HuggingFace (setting --gyro 1 shape-mismatches load_state_dict): (a) baseline_flexibility.md:28 matrix 'Sensor modality' cell 'fixed acc+gyro' -> 'fixed accel-only (3ch, as released; gyro path exists in code but no 6ch weights released)'; (b) baseline_flexibility.md:51 'Expects 6-axis (acc+gyro) per placed sensor' -> 'Expects 3-axis accelerometer per placed sensor (released checkpoint); the code has a --gyro path but no released 6-channel weights'; (c) references/baselines/unimts/citation.json note 'it consumes both accel AND gyro' -> 'paper describes 6-axis IMU, but the released checkpoint + default pretrain/eval scripts are accelerometer-only (3ch); gyro is sliced away unless --gyro 1, for which no weights are released'; (d) ALSO fix docs/v2/CROSSCHECK.md:161, which currently says 'UniMTS requires 3-axis accel + 3-axis gyro at 20 Hz' — change to accel-only. Operationally: treat UniMTS like ssl-wearables on the gyro axis — feed only the 3 accelerometer channels per placed sensor (or feed 6ch and rely on the default --gyro 0 slice); do not present UniMTS as gyro-bearing in the fairness narrative that contrasts it with ssl-wearables.

### [MEDIUM] MODELS-6: UniMTS (planned "cosine-tier" baseline, released HuggingFace — preprocessing / units
- [ ] **Issue:** UniMTS requires inputs normalized to m/s^2 (accel) and rad/s (gyro), and the conversion is per-dataset and NON-uniform. HALO's stored test sets are a mix of units, so a single conversion rule silently mis-scales half of them by ~9.8x, crippling a foundation model that expects a fixed physical scale.
  - **Evidence:** Computed from HALO parquet: |accel| mean motionsense=0.508, harth=1.004 (both in g); shoaib=9.858, realworld=9.860 (both m/s^2). UniMTS data.py applies ×9.80665 for motionsense (data.py:163) and harth (data.py:138), but NO conversion for shoaib (data.py:174) and realworld (data.py:194-201); README.md:54 'Normalize time series measurements (m/s^2 for acceleration)'. It also does gyro deg->rad (/180*pi) for some datasets (data.py:105,151,157). motionsense manifest: acc 'in g units'; shoaib/realworld ≈9.8 => already m/s^2.
  - **Fix:** In the (unbuilt) UniMTS CosineAdapter, do NOT apply one global accel scale. Reuse HALO's existing per-dataset unit map pattern from benchmark_data/scripts/preprocess_limubert.py (ACC_IN_G_UNITS={motionsense, harth, inclusivehar, capture24} -> x9.80665; ACC_IN_MILLI_G={opportunity} -> x9.80665/1000; shoaib, realworld, mobiact -> already m/s^2, no scaling), verifying each test set's stored units against its manifest/converter before choosing the factor. Apply gyro deg->rad only where the manifest declares deg/s (HALO's motionsense/shoaib/realworld gyro are already rad/s per their manifests, so no gyro conversion for those). Also match the gravity convention UniMTS was trained under (gravity-inclusive vs gravity-removed), since motionsense feeds gravity-removed userAcc while shoaib/realworld include gravity. Correct docs/v2/CROSSCHECK.md:190 which currently implies a uniform "carries accel in g -> standardize to m/s^2" blanket conversion that would over-scale shoaib/realworld by ~9.8x.

### [MEDIUM] MODELS-7: UniMTS (planned "cosine-tier" baseline, released HuggingFace — labels / eval-clarity
- [ ] **Issue:** Both HALO and UniMTS are text-aligned, so the cosine comparison must use IDENTICAL label strings or it measures prompt engineering, not architecture. HALO uses SentenceBERT over pre-registered underscored strings (e.g. 'walking_downstairs','cycling_sit','stairs_up'); UniMTS uses CLIP over ' '.join(curated label_dictionary words). CLIP's tokenizer handles underscores poorly, so feeding HALO's raw underscored labels would handicap UniMTS, while letting UniMTS use its private curated descriptions on the shared datasets would advantage it.
  - **Evidence:** data.py:240-242 'label_list = [" ".join(labels) for labels in label_dictionary.values()]; all_text = clip.tokenize(label_list)'; contrastive.py:24-38 encode_text is frozen CLIP ViT-B/32 text tower. HALO eval label files use underscored strings, e.g. benchmark_data/eval_v2/labels/harth.json labels ['cycling_sit','cycling_stand','stairs_down',...], motionsense ['walking_downstairs','walking_upstairs'].
  - **Fix:** Register a single normalization rule for the ENTIRE cosine (text-aligned) tier, not just the ConSE bridge: define one canonical human-readable string per class (underscores->spaces, e.g. 'walking downstairs') in benchmark_data/eval_v2/labels/*.json and route BOTH text encoders through it. Concretely: (1) In the protocol, add a cosine-tier row mirroring the existing bridge rule ('labels de-underscored') so UniMTS/NormWear are pinned to the same normalized string. (2) Fix the asymmetry the finding's fix implies but does not state: HALO's native path currently feeds RAW underscored labels (evaluate_tsfm_v2.py:118/206 via labels_ld=cfg['labels']); to make the comparison truly identical, HALO must consume the SAME normalized string (either de-underscore at eval, or store the canonical de-underscored form as the registered label and keep HALO robust via its BERT-subword pooling — validate HALO's own F1 is unaffected since it trained on underscored variants). (3) Add an explicit protocol clause forbidding any CosineAdapter from substituting or enriching the supplied L_D with its own curated label_dictionary/LLM descriptions on shared datasets — the adapter's encode_labels must encode exactly the canonical string it is given. This keeps the cosine comparison measuring architecture, not tokenizer/prompt engineering, while leaving each baseline's INPUT-format home turf (rate/channels, per baseline_flexibility.md) untouched.

### [MEDIUM] MODELS-8: UniMTS (planned "cosine-tier" baseline, released HuggingFace — patch-duration / window
- [ ] **Issue:** The flexibility matrix says UniMTS handles window length by 'resample to fixed length'. Actually it resamples to 20Hz (preserving duration) and then takes only the FIRST 10 s (200 samples), wrap-padding (tiling) the signal if shorter. So on HALO's shared 20Hz/120-timestep (6 s) window grid, UniMTS sees a 6 s window tiled to 10 s — never its native 10 s window — which is a home-turf deviation that must be chosen and documented consistently.
  - **Evidence:** data.py:222-233 'new_sampling_rate=20; new_length=...; resample(...); if resampled<padding: np.pad(..., mode="wrap"); real_inputs = resampled[:,:padding_size,:]'; padding_size default 200 (evaluate.py:101); README.md:68 'we will only [use] the first 10 seconds; padding will be automatically applied if shorter'. base.py:87 aligns outputs to label_20_120.npy (120 ts = 6 s @ 20Hz).
  - **Fix:** Two-part fix. (1) Correct the doc: change baseline_flexibility.md:28 "Window / session length" for UniMTS from "resample to fixed length" to something like "fixed 10 s (200 ts @ 20 Hz): first-10 s truncate + wrap-pad (tile) if shorter" and reclassify it under the "fixed" level (truncation/fabrication), NOT "resample". Extend the prose at :48-51 to state that UniMTS handles rate by resampling to 20 Hz but handles window length by taking the FIRST 200 samples (10 s) and wrap-tiling shorter signals — so long windows lose data and short windows get fabricated repeats. (2) Decide the window grid ONCE and document it: preferred option is to reuse HALO's existing shared 20 Hz / 6 s (120-ts) grid — feed UniMTS at 20 Hz so no double-resample occurs (new_length stays 120), let its wrap-pad tile 120->200, and keep GT alignment via label_20_120.npy; explicitly disclose that UniMTS is thereby run on a tiled 6 s->10 s window (off its 10 s home turf). Alternatively build a UniMTS-native 10 s grid with re-derived ground truth. Whichever is chosen, apply the SAME window definition across all models so ground-truth alignment is consistent, and record the choice in EVALUATION_PROTOCOL_V2.md / baseline_flexibility.md when the UniMTS adapter is built.

### [MEDIUM] MODELS-9: PLANNED baseline "NormWear" — faithfulness audit of referenc — sampling-rate / paper-fidelity
- [ ] **Issue:** The flexibility matrix labels NormWear 'flexible (CWT)' on sampling rate, but the released model resamples EVERY signal to a FIXED 65 Hz and its CWT scales are hardcoded in sample units (not physical Hz). NormWear is therefore a 'resample-to-65Hz' model on this axis (lossy, like ssl-wearables@30Hz and LiMU@20Hz), NOT rate-native/flexible. This overstates NormWear's flexibility and understates HALO's core uniqueness claim.
  - **Evidence:** baseline_flexibility.md:29 '| **NormWear** | flexible (CWT)‡ | ... | flexible (CWT)‡ |' and prose 52-55. Contradicted by released code: main_model.py `def wt(ts, lf=0.1, hf=65): signal.cwt(ts, signal.ricker, np.arange(lf, hf))` (scales fixed 0.1..65) and `self.sampling_rate = 65`; modules/signal_preprocess.py `preproc_all(all_tss, ss, ts=65, ...)` -> `basic_preproc(..., tr=ts)` resamples source rate ss down to target 65 Hz. Paper B.1 (normwear.txt:1612) 'we set the uniform sampling rate and interval length to 65 HZ and 6 seconds'. The ‡ footnote (line 33) already says 'verify against released code' — code inspection now RESOLVES it against the current value.
  - **Fix:** Change the NormWear sampling-rate cell in baseline_flexibility.md:29 from 'flexible (CWT)‡' to 'fixed 65 Hz (resample, lossy)', aligning it with the fix-upstream group (ssl-wearables 'fixed 30 Hz', LiMU 'fixed 20 Hz'), and cite paper App. B.1 ('uniform sampling rate ... 65 HZ') as the resolution of the ‡. In the prose (52-55), drop/qualify 'length/rate-adaptive' and state that NormWear resamples every signal to a fixed 65 Hz grid and applies CWT on fixed sample-unit scales (1-64), so sampling rate is handled in preprocessing exactly like the other fix-upstream baselines. Adjacent (worth fixing in the same edit but outside this finding's stated scope): the window/session-length cell on line 29 is also mislabeled 'flexible (CWT)' — the paper fixes a 6 s interval and zero-pads shorter samples, so it should read 'fixed 6 s (pad)'. These changes strengthen, not weaken, HALO's 'only model native on every axis' claim.

### [MEDIUM] MODELS-10: PLANNED baseline "NormWear" — faithfulness audit of referenc — preprocessing / plumbing
- [ ] **Issue:** get_embedding() only auto-resamples when the passed sampling_rate > 256 Hz. For HAR rates (20–100 Hz) it does NOT resample — it runs the 65Hz-tuned CWT directly on native-rate data. A naive adapter that trusts the sampling_rate argument to 'handle it' will silently feed off-distribution data (each CWT scale maps to the wrong physical frequency). A faithful adapter MUST pre-resample to 65 Hz and apply NormWear's full preprocessing before calling get_embedding.
  - **Evidence:** main_model.py get_embedding: `if sampling_rate != self.sampling_rate: if sampling_rate > 256: resampler = T.Resample(sampling_rate, self.sampling_rate) ...` — the resample branch is gated on >256 Hz only. The intended offline prep is modules/signal_preprocess.py `basic_preproc`: outlier removal (95th-pct diff), `impute(...tr=65)` resample-to-65, `signal.detrend`, `gaussian_filter(sigma=tr*0.02)` (=1.3), and `ts_clear /= np.mean(np.abs(ts_clear))` (mean-abs normalize, NOT z-score).
  - **Fix:** Before wiring the NormWear adapter, build benchmark_data/scripts/preprocess_normwear.py (parallel to preprocess_limubert.py) that emits a NEW 65 Hz NormWear copy for every test dataset, replicating NormWear's own preproc_all(ts=65)->basic_preproc(tr=65) pipeline per channel: (a) outlier removal at the 95th-pct first-difference threshold, (b) resample to 65 Hz (impute), (c) signal.detrend, (d) gaussian_filter(sigma=1.3), (e) mean-abs normalize (ts /= mean(|ts|)) — NOT z-score, (f) segment into 6 s / 390-sample windows. Then in the CosineAdapter, call model.get_embedding(x, sampling_rate=65) on this 65 Hz copy. Do NOT rely on passing the native sampling_rate to get_embedding: its resample branch is gated on >256 Hz only and the CWT uses a fixed 0.1-64 Hz (65 Hz-tuned) scale grid, so native-rate HAR data (20-100 Hz) would be silently off-distribution. Also correct docs/baselines/baseline_flexibility.md: change the NormWear "Sampling rate" cell from "flexible (CWT)" to effectively "fixed 65 Hz (pre-resample; resample only auto-triggers >256 Hz)", since the released code contradicts the current "flexible" characterization. Minor: apply tr=65 as NormWear does via preproc_all(ts=65), not by assuming basic_preproc's default tr (which is 125).

### [MEDIUM] MODELS-11: PLANNED baseline "NormWear" — faithfulness audit of referenc — eval-clarity / paper-fidelity
- [ ] **Issue:** NormWear's zero-shot is NOT a plain cosine CosineAdapter. It requires (a) a SEPARATE MSiTF alignment checkpoint, (b) a query-conditioned fusion (the sensor embedding depends on a task query sentence, so it is not the label-independent per-window embedding the CosineAdapter contract assumes), (c) label encoding via a Clinical-TinyLlama LLM (2048-dim, mean-pooled) — NOT SentenceBERT, and (d) L1/Manhattan-distance scoring — NOT cosine. Reusing HALO's SentenceBERT+cosine CosineAdapter would put labels in the wrong embedding space and score with the wrong metric, yielding ~random zero-shot and an unfairly bad NormWear result.
  - **Evidence:** zero_shot/msitf_fusion.py:228-229 `self.tokenizer = AutoTokenizer.from_pretrained('muzammil-eds/tinyllama-2.5T-Clinical-v2')`, `self.nlp_model = ...AutoModelForCausalLM.from_pretrained('muzammil-eds/tinyllama-2.5T-Clinical-v2')`; txt_encode returns mean_pooling(...)-> 2048-d. zero_shot/zero_shot_inference.py `zs_evaluate`: `distances = torch.abs(sensor_embeds[:,None,:] - choice_embeds[None,:,:]).sum(dim=-1)` (L1) and `load_model` needs `normwear_msitf_..._checkpoint-5.pth` in addition to the backbone. base.py CosineAdapter contract (lines 61-71) only exposes label-independent `window_embeddings` + `encode_labels`, cosine-scored by run_baselines_v2.
  - **Fix:** Do NOT wire NormWear as a plain CosineAdapter. Build a bespoke adapter that: (1) in setup() loads BOTH released checkpoints (normwear_pretrain_ckpt.pth backbone + normwear_msitf_*_checkpoint-5.pth alignment); (2) produces per-window MSiTF sensor embeddings conditioned on a fixed HAR task query (e.g. add a HAR entry to NormWear's sentence_template: query 'What activity is the subject doing?', answer template 'This subject is currently {}') — these remain label-independent (N,D) so they can populate window_embeddings; (3) implements encode_labels with NormWear's OWN txt_encode (Clinical-TinyLlama, 2048-d mean-pooled), never HALO's SentenceBERT; (4) scores by L1/Manhattan (argmin of torch.abs(sensor-choice).sum(-1)), NOT cosine. Because run_baselines_v2.py:48-50 hardwires cosine (emb @ text.T -> argmax) for the cosine tier, either add an L1/'distance' scoring branch keyed on adapter.tier in the driver, or have the adapter return pre-argmin'd predictions. Also correct the docs so they don't steer toward the wrong integration: update baseline_flexibility.md and citation.json to note NormWear's home-turf metric is L1 (not cosine) and its label encoder is Clinical-TinyLlama (not SentenceBERT), and document the L1-vs-cosine deviation explicitly as NormWear's native scoring for fairness.

### [MEDIUM] MODELS-12: PLANNED baseline "NormWear" — faithfulness audit of referenc — fairness / paper-fidelity
- [ ] **Issue:** Pretraining-corpus DOMAIN asymmetry. NormWear's released weights are pretrained almost entirely on non-IMU physiological signals (PPG/ECG/EEG/GSR/PCG); only PPG-Dalia contributes any IMU. It has essentially never seen 6-axis phone/watch HAR. HALO trains on 11 HAR IMU datasets (~400h). A zero-shot HAR head-to-head therefore compares an in-domain specialist (HALO) against an out-of-domain general physiological FM (NormWear); a HALO win is confounded by training-corpus domain match, not by tokenizer quality. Note also the '14,943 h' headline is an augmented per-channel count — real distinct pretraining data is ~385 h (Table 5 segments), same order as HALO's ~400 h — so the asymmetry is DOMAIN, not raw hours.
  - **Evidence:** Paper Table 5 (normwear.txt:1446-1499): pretrain sets = Cuff-Less-BP, PPG-Dalia, Auditory-EEG, PhyAAt, MAUS, Mendeley-YAAD, Brain-Cognitive, EPHNOGRAM, BIDMC; '#Segm 230,962(385h)', '# Segm w/ Augment 2,576,418(4,294h)', '# Sign w/ Augment 8,965,538(14,943h)'. Modality mix is PPG/ECG/EEG/GSR/PCG with IMU only via PPG-Dalia.
  - **Fix:** Endorse the proposed fix. Before the NormWear head-to-head is published, add a disclosure in docs/baselines/RESULTS_V2.md (and extend the NormWear note in docs/baselines/baseline_flexibility.md, currently only about CWT tokenizer flexibility) stating: (a) NormWear is applied OUT OF DOMAIN — a physiological foundation model (PPG/ECG/EEG/GSR/PCG) run on IMU-HAR; per Table 5, IMU appears in only 1 of its 9 pretraining datasets (PPG-Dalia, ~71h / ~18% of segments, 3-axis wrist accel, no gyroscope), so it has essentially never seen 6-axis phone/watch HAR; (b) its distinct pretraining volume is ~385h (Table 5 '# Segm', corroborated by Appendix A), comparable to HALO's ~400h — the widely-quoted '14,943h' is the augmented per-signal/per-channel count, NOT distinct hours, so the gap is DOMAIN, not raw hours; (c) frame the result as 'closest tokenizer contender, evaluated out-of-domain' rather than an equal-footing tokenizer A/B, since a HALO win jointly reflects pretraining-domain match plus tokenizer quality. Optionally note that any citation of NormWear's data scale should use ~385h distinct, not 14,943h.

### [MEDIUM] MODELS-13: PLANNED baseline "ssl-wearables" (OxWearables harnet; SSL Re — preprocessing
- [ ] **Issue:** harnet expects tri-axial input in units of g WITH gravity retained (UK-Biobank/Axivity calibration). The only existing eval pipeline is BENCH_LIMU at 20 Hz in m/s² (preprocess_limubert multiplies g-unit datasets by 9.80665). Reusing it would feed harnet values ~9.8x too large AND at the wrong rate (20 Hz then up-interpolated to 30 Hz cannot recover 20-30 Hz content). Neither baseline_flexibility.md nor citation.json states the g-unit / gravity-present requirement, so an adapter author is likely to reuse the 20 Hz m/s² npy — a silent, catastrophic handicap.
  - **Evidence:** benchmark_data/scripts/preprocess_limubert.py header: 'Accelerometer values must be in m/s² (the LIMU-BERT training pipeline divides by 9.8)'; ACC_IN_G_UNITS conversion ×GRAVITY_MS2. datascripts/capture24/convert.py:197 'acceleration in g (INCLUDES gravity)'. paper.pdf Table1 'UK-Biobank ... Free-living' wrist Axivity (g, gravity present). val_scripts/.../evaluate_limubert.py:168 normalize_for_limubert only divides acc by 9.8 (no per-window standardization that would otherwise cancel scale).
  - **Fix:** Endorse the finding's fix with one scoping refinement. The 30 Hz / 3-ch-from-raw pipeline is already mandated by baseline_flexibility.md, so the actionable delta is the units + gravity requirement:

1. When the ssl-wearables adapter is built, derive its input FROM RAW CSVs (benchmark_data/raw/…), not from benchmark_data/processed/limubert (which is 20 Hz and already ×9.80665 into m/s²): resample raw to 30 Hz, keep acc x/y/z only, express in g (do NOT multiply by 9.80665), keep gravity (no high-pass/DC removal), output channels-first (N, 3, 300).
2. Add an explicit "g-units, gravity-present, 30 Hz, 3-ch, 300-sample" requirement for ssl-wearables/harnet to docs/baselines/baseline_flexibility.md and EVALUATION_PROTOCOL_V2.md (and optionally citation.json notes), so no adapter author reuses the m/s² values or applies the LIMU-BERT-style ×9.8 conversion by analogy.
3. Sanity-check at adapter time: a stationary window should read ~1 g magnitude, not ~9.8. Assert input abs-median is O(1), not O(10), before inference.

### [MEDIUM] MODELS-14: PLANNED baseline "ssl-wearables" (OxWearables harnet; SSL Re — pretraining-corpus-asymmetry
- [ ] **Issue:** ssl-wearables enters via released weights pretrained on UK-Biobank ~700,000 person-days / ~100k subjects of wrist accelerometry — vs HALO's ~400 h / 11 datasets. This is an ADVANTAGE for the baseline (not unfair to HALO), and UK-Biobank is not available to HALO, but it is undocumented; without disclosure a HALO zero-shot win looks larger than it is and a loss looks worse. Note also the paper's alternate Capture-24-pretrained variant exists (Capture-24 is a HALO TRAIN set) — if used it stays symmetric, but the checkpoint choice must be stated.
  - **Evidence:** paper.pdf: '700,000 person-days of unlabelled ... UK-Biobank'; Table1 'UK-Biobank ∼100K 6 B Unlabelled Free-living'. citation.json notes: 'Additional pretrained variants (Capture-24, Rowlands, 1k-subject subsets) hosted on project server.'
  - **Fix:** When the ssl-wearables adapter is built, add a pretraining-corpus disclosure to both baseline_flexibility.md and RESULTS_V2.md: a footnote/column stating ssl-wearables uses released harnet weights pretrained on ~700k person-days of UK-Biobank wrist accelerometry (~100k subjects) vs HALO's ~400 h / 11 datasets — a ~3-4 order-of-magnitude asymmetry that favors the baseline and is not available to HALO. Pin and record the exact released checkpoint in the adapter and in the output JSON provenance (recommend the default UK-Biobank harnet10, class_num set for the ConSE closed-vocab head): this keeps the comparison clean and avoids the contamination trap of the alternate Capture-24-pretrained variant, since Capture-24 is a HALO TRAIN set (if that variant is ever used, it must be explicitly flagged as train-set overlap). This mirrors the DEBUG_SWEEP recommendation to log the exact per-model input/weights provenance in the results JSON. Optionally correct residual "from-scratch" phrasing (already partially flagged in CROSSCHECK.md:205) to "HAR-specific SSL-pretrained foundation model." Note the finding's directional framing is imprecise (disclosure arguably makes a HALO win MORE impressive, not smaller) but the disclosure itself is warranted regardless.

### [MEDIUM] MODELS-15: PLANNED baseline "deepconvlstm" (Ordóñez & Roggen 2016, Sens — fairness
- [ ] **Issue:** The from-scratch floor is the single easiest baseline to accidentally HANDICAP (depressed floor makes HALO look better) OR ADVANTAGE. Handicap: a ~3.9M-param CNN+2xLSTM trained on 1% of a small HAR dataset with a thrown-together recipe (bad normalization/LR/epochs, no class balancing/early-stopping) collapses to near-zero macro-F1 for reasons unrelated to the method. Advantage: if it silently sees more than the 1%/10% split, touches val subjects, or gets per-dataset-tuned windows/rate that HALO doesn't.
  - **Evidence:** citation.json role='supervised from-scratch few-shot floor ... 1%/10% subject-disjoint'. No hyperparameter recipe is documented anywhere in refs/plan. HALO's FS uses a fixed disciplined recipe: FINETUNE_EPOCHS=20, PATIENCE=5, class-balanced subsample, best-val checkpointing (eval_common.py:101-106; evaluate_tsfm_v2.py:197-258).
  - **Fix:** Adopt the proposed fix with one emphasis correction. Split/subsample half is already pre-registered — just reuse ev2.subject_disjoint_split(subjects, seed=3431) + ev2.balanced_subsample_indices(rate) verbatim when the adapter is built (do NOT re-implement or re-tune the split). The real deliverable is a pinned, published-faithful, competently-tuned FROM-SCRATCH training recipe that is explicitly NOT HALO's 20-epoch/1e-5 fine-tune recipe: Ordonez and Roggen architecture (4 conv x 64 filters 5x1, 2 LSTM x 128, dropout 0.5, softmax); per-channel z-norm on TRAIN stats only; class-balanced batches; RMSProp/Adam at a from-scratch-appropriate LR; enough epochs with early stopping on the subject-disjoint val split (not capped at HALO's 20). Document it in EVALUATION_PROTOCOL_V2.md alongside the existing pre-registered constants. Report full-shot (100%) alongside FS-1/FS-10 so a low floor is attributable to few-shot data scarcity, not a broken recipe or a capacity claim.

### [MEDIUM] MODELS-16: PLANNED baseline "deepconvlstm" (Ordóñez & Roggen 2016, Sens — preprocessing
- [ ] **Issue:** The plan does not specify DeepConvLSTM's exact input format: which windowed raw array it consumes (the 20 Hz / 6-ch pad+mask limubert arrays vs a native-rate export), the window length, or the sampling rate. Because it is retrained from scratch, ANY of these 'works' — which is exactly why an undocumented choice is a fairness hole (its receptive field / rate is a free parameter that co-determines the result).
  - **Evidence:** base.py:28 BENCH_LIMU is '20 Hz baseline data'; limubert.py:30 reads data_20_120.npy = (N,120,6). No raw-data source is wired for a from-scratch baseline; the HALO FS path receives `raw_data` externally (evaluate_tsfm_v2.py:179,221). The paper's canonical window is 24 frames @ 30 Hz (0.8 s), not 120 @ 20 Hz.
  - **Fix:** Before building the adapter, pin and disclose DeepConvLSTM's input in baseline_flexibility.md and EVALUATION_PROTOCOL_V2.md. Preferred: give it the SAME per-window arrays the other closed-vocab few-shot baselines get — 20 Hz, 120 timesteps, 6-ch acc+gyro from benchmark_data/processed/limubert/<ds>/data_20_120.npy — holding rate/window/channels fixed across all FS models for cross-model comparability. If instead you adopt the paper's canonical ~24-frame/30 Hz window, apply that single value uniformly to every dataset and explicitly disclose the receptive-field difference in a capability-delta note. Either way, forbid dataset-optimized window/rate: record the chosen (rate, window_ts, channels) as a pre-registered constant in the protocol's "Pre-registered constants (do not tune post-hoc)" table, alongside the input source (20 Hz limubert arrays vs native-rate export) so the choice is unambiguous and auditable.

### [MEDIUM] MODELS-17: HALO v2 evaluation clarity/meaningfulness: docs/baselines/RE — eval-clarity/staleness
- [ ] **Issue:** RESULTS_V2.md and EVALUATION_PROTOCOL_V2.md report the 6-dataset benchmark as {motionsense, realworld, mobiact, shoaib, opportunity, harth}, but the LIVE benchmark defined in code is {..., harth, inclusivehar} with opportunity DEMOTED. Every average, CI, and narrative in the two headline docs is on a benchmark the code no longer produces. The headline avg 33.2 includes opportunity (34.2) which was demoted, and omits inclusivehar (native f1_macro 20.5) which is the actual 6th set. The FS table (line 89-97) is also still on opportunity. inclusivehar is never even mentioned in either doc.
  - **Evidence:** generate_eval_v2_labels.py:45-53 `# opportunity demoted to appendix 2026-07 ... EVALUATED_DATASETS = [motionsense, realworld, mobiact, shoaib, harth, inclusivehar]`; assemble_v2_table.py:7 and run_baselines_v2.py:30 both `DATASETS = [...,"harth","inclusivehar"]`; vs RESULTS_V2.md:10 `... shoaib, opportunity, **harth**` and :33 `average (6) | 33.2`; EVALUATION_PROTOCOL_V2.md:23-24 lists opportunity. tsfm_v2_inclusivehar.json inclusivehar native f1_macro=20.5, parity=28.3 (never in RESULTS_V2).
  - **Fix:** Regenerate both headline docs on the current inclusivehar-in/opportunity-out set. In RESULTS_V2.md: fix the '6 datasets' sentence (line 10) to list inclusivehar instead of opportunity; replace the opportunity row in the ZS-XD table and ZS-vs-baseline table with an inclusivehar row (native f1 20.5, parity 28.3, plus its bAcc/acc/CI from tsfm_v2_inclusivehar.json); recompute native avg to ~30.9 and parity avg to ~29.1; move opportunity to an explicitly-labeled appendix row (34.2/31.8); update the capability-Delta and 'leads the 6-dataset average' narrative to the recomputed number; update the FS table (lines 89-97) to inclusivehar. Add an inclusivehar ability-gap sub-row from inclusivehar_ability_stratified.json (able_bodied 25.5 vs disabled 18.2, gap 7.3). In EVALUATION_PROTOCOL_V2.md: fix the pre-registered test-set sentence (lines 23-24) to name inclusivehar and note opportunity is demoted to an appendix, resolving the internal contradiction with the EVALUATED_DATASETS pointer on lines 29-31. Also note baselines must be (re-)scored on inclusivehar before any 'HALO leads' claim on the new set is asserted. (CLAUDE.md's Test-(6) list is likewise stale but out of scope of these two files.)

### [MEDIUM] MODELS-18: HALO v2 evaluation clarity/meaningfulness: docs/baselines/RE — misleading-aggregate
- [ ] **Issue:** The `avg` column averages over whichever cells happen to be present, so different models' averages are computed over DIFFERENT dataset subsets yet placed in the same column and directly compared. A model with data on only its strongest (or only) dataset gets an `avg` that looks comparable to HALO's full-set avg. Today CrossHAR's `avg 22.1` is a 1-dataset (shoaib) mean sitting next to HALO's 5-dataset `avg 32.9`. This makes the central comparison apples-to-oranges by construction.
  - **Evidence:** assemble_v2_table.py:43-45 `avail = [v for v in f1s.values() if v is not None]; avg = sum(avail)/len(avail)` — denominator is per-model available count, not the fixed benchmark size. Confirmed in the live table output above (CrossHAR avg over 1 cell, HALO over 5).
  - **Fix:** In assemble_v2_table.py, never emit a same-column average for a model whose row is incomplete. Track present-cell count n vs len(DATASETS); if n < len(DATASETS), render the avg cell as e.g. `— (partial n/6)` (or `22.1 (n=1/6)` if a value is still wanted) so partial averages are never placed unannotated beside complete ones. Do NOT 'fix' by averaging over the fixed 6 with F1=0 for un-run datasets — a missing run is not a zero score; annotate-with-n or suppress is correct. Also reconcile the DATASETS list (inclusivehar) with the committed JSONs (opportunity) so LiMU-BERT's opportunity cell is not silently dropped, which currently compounds the misleading single-cell average.

### [MEDIUM] MODELS-19: HALO v2 evaluation clarity/meaningfulness: docs/baselines/RE — fairness/eval-clarity
- [ ] **Issue:** No parameter-count disclosure anywhere in the results table or either doc, so the reader cannot see HALO's largest (dis)advantage: HALO is ~26M active params vs CrossHAR's ~62.6K encoder (per the known-gap) and LiMU-BERT's small GRU — a ~400x capacity gap. The table's only context column is `tier`. This also makes the parity row's framing an overclaim: the doc calls it 'the same inputs the baselines get ... the architecture-only claim' (RESULTS_V2:77-78), but parity equalizes only INPUT (20 Hz + neutral text), not capacity — HALO is still a 26M model beating ~60K-param baselines.
  - **Evidence:** grep for `param|26M|62.6|531` in RESULTS_V2.md + EVALUATION_PROTOCOL_V2.md returns nothing. Table header RESULTS_V2.md:53 has no params column. Known-gap: CrossHAR encoder ~62.6K params; HALO ~26M active (repo header / debug-sweep ~25.8M).
  - **Fix:** Add a trainable-params column to the ZS-XD vs-baselines table in RESULTS_V2.md (and emit it from assemble_v2_table.py): HALO (Small-Deep) ~25.8M active (note 35.1M instantiated with 9.3M dead/gated — use the honest active count), CrossHAR ~62.6K encoder, LiMU-BERT its GRU size, with a footnote that the frozen SBERT text encoder (all-MiniLM-L6-v2, ~22.7M) is SHARED across all text-aligned/ConSE-bridged models and excluded to avoid double-counting. Reword the parity-row bullet (RESULTS_V2.md:76-78) from "the architecture-only claim" to "input-parity, not capacity-parity: this equalizes sampling rate and channel text, but HALO still carries ~400x the trainable parameters of CrossHAR, so the residual gap reflects capacity+architecture, not architecture alone." Mirror the disclosure in EVALUATION_PROTOCOL_V2.md's Fairness-rows section (state that parity controls input format only, not model scale). Ideally add a param-matched or small-HALO row to isolate architecture from scale; if that ablation is not yet run, mark it as a pending row rather than leaving the "architecture-only" wording.

### [LOW] MODELS-20: PLANNED baseline "NormWear" — faithfulness audit of referenc — fairness / eval-clarity
- [ ] **Issue:** Eval-set contamination check — CLEAN, but must be recorded. None of HALO's 6 held-out test sets (motionsense, realworld, mobiact, shoaib, harth, inclusivehar) appear in NormWear's pretraining (Table 5) or its downstream eval (Table 4). NormWear DID report on UCI-HAR, which is one of HALO's TRAIN datasets (not a test-set leak for HALO, but both models have 'seen' UCI-HAR in some capacity — HALO trains on it, NormWear evaluated on it).
  - **Evidence:** Paper Table 4 downstream (normwear.txt:1337-1445) = WESAD, UCI-HAR, DriverFatigue, Epilepsy, GAMEEMO, ECG-Abnormal, PPG-BP, PhysioNet EMG, Noninvasive-BP, PPG-Hgb, Fetal-fPCG; Table 5 pretrain listed above. HALO test sets absent from both. UCI-HAR present in Table 4 and HALO TRAIN list.
  - **Fix:** Record the verified-clean integrity check in the results/eval notes (e.g. RESULTS_V2.md or a NormWear adapter-build note): "NormWear released weights (v1.0.0-alpha, arXiv:2412.09758): none of HALO's 6 held-out test sets (motionsense, realworld, mobiact, shoaib, harth, inclusivehar) appear in NormWear's pretraining (Table 5) or downstream eval (Table 4) — no eval-set contamination for the head-to-head. Only UCI-HAR overlaps: it is a HALO TRAIN dataset and a NormWear DOWNSTREAM probe (Table 4, explicitly 'unseen during pretraining'), so NormWear's frozen weights never even trained on it — no weight-level contamination, and it is not a HALO test set, so it does not bias the comparison." No code or protocol change needed; documentation only. When the adapter is actually built, re-verify against the released repo's pretraining manifest (not just the paper tables), since the check must ultimately reflect the checkpoint used.

### [LOW] MODELS-21: PLANNED baseline "ssl-wearables" (OxWearables harnet; SSL Re — fairness
- [ ] **Issue:** The released harnet is a frozen SSL trunk with an UNTRAINED head; ConSE requires a softmax over the 87 global training labels, so the adapter must train a classifier on HALO's combined_train. The paper's BEST protocol fine-tunes ALL layers (explicitly beats freezing). For parity with HALO's frozen zero-shot AND with the limubert/crosshar adapters (which freeze the encoder and train only a small head), ssl-wearables MUST freeze the trunk and train the head only. If the adapter fine-tunes the trunk end-to-end it silently gains an undocumented advantage HALO never gets. This freeze requirement is documented nowhere.
  - **Evidence:** paper.pdf: 'freezing the trunk (feature extractor) and fine-tuning' vs 'fine-tuning all the layers ... Fine-tuning all layers was better than fine-tuning just the fully connected (FC) layers'. baselines/limubert.py docstring: 'frozen LiMU-BERT encoder + cached GRU classifier'. citation.json: 'Feature extractor is pretrained; classifier head is untrained and requires fine-tuning.'
  - **Fix:** When building baselines/ssl_wearables.py (ConSEAdapter): load torch.hub 'OxWearables/ssl-wearables' harnet10 pretrained=True, freeze the ResNet-V2 trunk (eval() + no_grad, no requires_grad), and train ONLY a head (paper's 512/1024-d FC before softmax) over the 87 global labels on the 30 Hz/g-normalized combined_train, caching it like test_output/baseline_evaluation/{limubert_zs_gru.pt,crosshar_zs_transformer.pt}; ConSE-bridge the resulting softmax to each test set's L_D. Explicitly document in EVALUATION_PROTOCOL_V2.md (ConSE section) the general rule that all ZS-XD ConSE classifiers are trained on FROZEN pre-extracted embeddings (trunk frozen, head-only, no end-to-end fine-tune), and add an ssl-wearables note that this deliberately departs from the paper's best recipe (fine-tune all layers) for within-tier parity with limubert/crosshar. Frame the parity as within-ConSE-tier rather than "parity with HALO's frozen zero-shot" (HALO's encoder is trained on combined_train, not frozen). The FS-1%/10% rows remain end-to-end for all models as already documented.

### [LOW] MODELS-22: PLANNED baseline "ssl-wearables" (OxWearables harnet; SSL Re — paper-fidelity
- [ ] **Issue:** harnet was pretrained EXCLUSIVELY on wrist-worn, gravity-present free-living data. baseline_flexibility.md marks 'Placement-aware: none' (correct) but does not note the wrist-only / gravity-present training domain. HALO test sets include waist/thigh/chest/pocket placements and at least one gravity-removed test set (motionsense = iOS userAcceleration, gravity removed by Core Motion), all of which are out-of-distribution for harnet. These are faithful handicaps to disclose, not to 'fix' by fabricating gravity or re-orienting axes.
  - **Evidence:** paper.pdf: 'tri-axial accelerometer data from wrist-worn activity trackers'. datascripts/process_motionsense.py:17 'userAcceleration is in g units (gravity removed by Core Motion)'; DATA_FORMAT.md notes datasets with 'gravity removed'. baseline_flexibility.md:26 lists placement 'none' but no wrist/gravity domain note.
  - **Fix:** In docs/baselines/baseline_flexibility.md, extend the ssl-wearables disclosure (the bullet at lines 43-45, and optionally a "training domain" note in the matrix) to state that harnet was pretrained EXCLUSIVELY on wrist-worn, RAW (gravity-present) UK-Biobank AX3 accelerometry at 30 Hz/10 s. Then disclose the resulting inherent, faithful per-test-set OOD handicap: every non-wrist test placement (motionsense/mobiact trouser pocket, realworld thigh/chest/waist/forearm, harth thigh+lower-back, inclusivehar waist, shoaib multi-position) and the gravity-removed motionsense stream (userAcceleration, gravity stripped by Core Motion) are out-of-distribution for harnet. Mirror the existing convention already used for its gyro handicap ("...disclose"). Explicitly note this is to be reported as an inherent handicap when the (still-unbuilt) adapter runs — NOT to be "fixed" by synthesizing gravity onto gravity-removed sets or re-orienting axes, per the no-silent-handicap fairness principle. No code change is required since the adapter is not yet built; this is a documentation-completeness addition to the fairness matrix.

### [LOW] MODELS-23: HALO v2 evaluation clarity/meaningfulness: docs/baselines/RE — eval-clarity/metric-comparability
- [ ] **Issue:** macro-F1 is averaged over each model's OWN GT∪predicted class set, so on the same dataset two models are scored over different-sized class sets. A model that routes false positives into extra zero-window candidate classes is averaged over a larger denominator (each such class contributes F1=0), so a lower cross-model macro-F1 can partly reflect predicting into more empty candidates rather than worse recognition. The protocol justifies the rule for a single model but never flags the cross-model comparability caveat, and the table doesn't surface the per-cell scored-class count (the JSONs record n_scored_classes but it's hidden).
  - **Evidence:** EVALUATION_PROTOCOL_V2.md:47-53 defines macro-F1 over `GT-classes ∪ predicted-classes`; RESULTS_V2.md:19-23 shows this changes HALO's HARTH 19.8→13.2 purely from FPs into 4 zero-window candidates — i.e. HALO is averaged over more classes than a baseline that never predicts them. JSONs carry `n_scored_classes` per cell but it is not shown in the table.
  - **Fix:** In EVALUATION_PROTOCOL_V2.md add a cross-model comparability caveat to the Metrics section: state that because the macro-F1 denominator is GT∪(that model's own predictions), two models on the same dataset can be averaged over different-sized class sets, so a lower cross-model macro-F1 can partly reflect predicting into more zero-window candidate classes (which add F1=0 cells) rather than worse recognition of present classes — this bites specifically on harth (the only dataset with zero-window candidate classes in L_D). Then surface the denominator: add a per-cell n_scored_classes column (or footnote) to the RESULTS_V2 tables by plumbing the already-stored n_scored_classes through assemble_v2_table.py (currently reads only f1_macro at line 42). For a strictly comparable cross-model table, optionally fix a common per-dataset scoring class set for all models (e.g. full L_D, or GT ∪ union-of-all-models'-predictions frozen once) so every cell shares one denominator; note union-of-all-predictions is non-monotone as models are added, so full-L_D or explicit disclosure + the n_scored_classes column is the cleaner minimal fix.


## EVAL (2)

### [MEDIUM] EVAL-1: eval_v2 fairness across baselines (val_scripts/human_activit — paper-fidelity / parity-claim
- [ ] **Issue:** The documented parity row promises every model the IDENTICAL anti-aliased 20 Hz signal via scipy.signal.resample_poly, but baselines are never fed resample_poly data. Baselines consume bin-and-mean 20 Hz data (a different anti-aliasing filter) AND a g->m/s2 unit conversion on 3 of the 6 test sets, while HALO-parity is fed resample_poly on native-unit windows. The parity row therefore does not isolate architecture at the signal level, yet it is the row used to attribute HALO's margin to architecture.
  - **Evidence:** EVALUATION_PROTOCOL_V2.md:89-91 'every model receives the identical anti-aliased 20 Hz signal (scipy.signal.resample_poly) + neutral channel text'. Baseline 20 Hz data is built by preprocess_limubert.py:63-114 downsample_by_averaging (bin-and-mean, NOT resample_poly) with acc g->m/s2 at :266-269 (ACC_IN_G_UNITS={motionsense,inclusivehar,harth} among test sets). HALO-parity uses evaluate_tsfm_v2.py:70-75 resample_windows->resample_poly on native units (no unit conversion). So HALO-parity and baselines differ in both anti-aliasing method and amplitude scale on motionsense/inclusivehar/harth.
  - **Fix:** Reword EVALUATION_PROTOCOL_V2.md:89-91 to drop the "identical anti-aliased 20 Hz signal (scipy.signal.resample_poly)" claim. State instead: parity = matched nominal 20 Hz sampling + neutral channel text, with each model kept in its own required normalization/anti-aliasing (baselines: bin-and-mean 20 Hz + LiMU-BERT's g-scale normalization, i.e. m/s²/9.8 with the g→m/s² step for motionsense/inclusivehar/harth; HALO: polyphase resample_poly on native-unit windows). Explicitly note that byte-identical signals are impossible across heterogeneous architectures (baselines need m/s²/9.8, HALO's filterbank needs its trained native units), so the parity row controls nominal rate and channel-text, not the exact waveform. Do NOT force HALO onto the m/s² bin-and-mean tensor (that would mis-scale its filterbank and change the anti-aliasing). Optionally align the two docs by cross-referencing baseline_flexibility.md:39 ("bin-and-mean resampled to 20 Hz"), which already describes the baseline path correctly.

### [MEDIUM] EVAL-2: eval_v2 fairness across baselines (val_scripts/human_activit — fairness / eval-clarity
- [ ] **Issue:** The aggregate 'avg' column is computed over each model's own AVAILABLE datasets, so a model missing any dataset (an N/A cell) is averaged over a smaller support and printed side-by-side with HALO's 6-dataset average as if comparable. This is exactly the ssl-wearables-on-gyro / DeepConvLSTM-ZS N/A scenario the protocol must handle: a model that can only run 2 of 6 sets could show a higher 'avg' than HALO purely from an easier subset.
  - **Evidence:** assemble_v2_table.py:43-44 'avail = [v for v in f1s.values() if v is not None]; avg = sum(avail)/len(avail)'. Missing datasets render as em-dash (line 53) but the avg denominator shrinks silently. No intersection/common-subset alignment anywhere.
  - **Fix:** In assemble_v2_table.py, do not place averages over different supports in the same column. After building all rows, compute the common set of datasets present for every compared model — common = [d for d in DATASETS if all(f1s[d] is not None for _,_,f1s,_ in rows)] — average each model only over `common`, and label the column with its N (e.g. '**avg (N=%d)**' % len(common)). Optionally add a separate, explicitly-labeled per-model 'own-support avg (n sets)' column showing the N each model actually covers, so a 1-set number is never printed under the same header as a 6-set number. Additionally, emit a visible warning (or refuse to render) when model supports differ, so a partial/debug JSON (like the current 1-dataset CrossHAR/LiMU-BERT files) can never silently produce an incomparable 'avg' next to HALO's full-support average.

---

## Resolution log (2026-07-10)

Status legend:
- ✅ **FIXED** — code/config/doc changed; verified by re-run, unit tests, or direct measurement where possible.
- 🔶 **CODE-FIXED (raw absent)** — converter code corrected for the eventual re-fetch, but **raw data is not present locally** (`data/raw/{recgym,kuhar,mhealth}` are empty), so it could not be re-run/re-verified this pass. Only `pamap2` and `hhar` have local raw.
- 🚩 **FLAGGED (no clean fix)** — honestly documented in the manifest + here; a correct fix needs a clean re-fetch. Per explicit instruction, these are flagged, **not fabricated**.
- ⏳ **DEFERRED** — build-time (baseline adapter not yet built) or Phase-4 (post-retrain re-score / doc regeneration). Not a protocol-survivable bug that a retrain wouldn't subsume.

### DATA (26)
| # | Sev | Status | Note |
|---|---|---|---|
| DATA-1 | HIGH | ✅ FIXED+VERIFIED | hhar re-converted with gap-split `resample_stream`; sessions 318,543→47,961; standing 64%→16.6%; dead-flat 99.4%→0%. |
| DATA-2 | HIGH | ✅ FIXED (root cause) | Root cause (fabricated hhar stream) removed by DATA-1 re-conversion; per-class/subject counts now balanced. Optional stratified-cap in loader left as a nicety (uniform cap is no longer catastrophic post-reconvert). |
| DATA-3 | MED | ✅ FIXED+VERIFIED | Same gap-guard; stair-class flat fraction 16–23%→~0%. |
| DATA-4 | MED | ✅ FIXED | `main()` now clears `sessions/` before writing; on-disk == labels.json (47,961); 143k orphans gone. |
| DATA-5 | MED | ✅ FIXED | Dropped `'ori'` from `IMU_PATTERNS` at **both** `get_sample` and `get_channel_counts` — 12 invalid PAMAP2 orientation channels excluded. |
| DATA-6 | MED | ✅ FIXED+VERIFIED | `pamap2 create_manifest()` now **byte-equal** to on-disk manifest (acc "m/s^2 including gravity", ori quaternion wording, "12 protocol activities"). |
| DATA-7 | MED | ✅ FIXED | `dataset_config.json` dsads `lying_side`→`lying_right_side`. Derived artifacts (metadata.json, limubert mappings) regenerate in the retrain/export pipeline. |
| DATA-8 | MED | ✅ FIXED+VERIFIED | kuhar acc gravity-removed confirmed empirically (static \|acc\|≈0.06); manifest + `create_manifest()` now say "gravity removed / linear acceleration". |
| DATA-9 | MED | 🔶 CODE-FIXED (raw absent) | Added robust per-file rate estimation from timestamps + windowing at true rate. Empirically the converted data already carries true timestamps (100 Hz for all subjects; the "126 Hz" was subject **1016 @ 111 Hz**, handled natively). Full re-run blocked on raw. |
| DATA-10 | MED | 🚩 FLAGGED | Subject **1101** (out of 1001–1090 range, 8.7% of windows, correct 100 Hz) is a volume anomaly, not a rate one. Added out-of-range + subject-share guards to the converter; can't quarantine without raw. Retrain-tolerable (train set; subject-disjoint). |
| DATA-11 | MED | ✅ FIXED+VERIFIED | `kuhar create_manifest()` regenerated to match on-disk (placement `(waist)`, 17 activities) + gravity note; `num_subjects` corrected 90→**89** (true count). |
| DATA-12 | MED | ✅ FIXED+VERIFIED | hapt↔uci_har split co-assigned to one `uci_har_family` key — subject-disjoint guarantee now holds across the duplicate pair (68 loader tests pass). De-dup of the 2069 basic-class sessions (part B) left as optional corpus trim. |
| DATA-13 | MED | 🚩 FLAGGED | mhealth **gyro** degeneracy confirmed (\|gyro\|≈0.99, 72.7% sample-and-hold, barely activity-correlated). Flagged in 6 gyro channel descriptions (code + manifest). **acc + mag verified clean** (0.0% / 1.2% hold) — contradicts the finding's "mag behaves like a gyro" claim. No fabrication; exclusion recommended as follow-up. |
| DATA-14 | MED | ✅ FIXED | `_gravity_present` now prefers the documented gravity state from channel text and hardened its signal fallback — recgym no longer misfires gravity/yaw aug. (recgym acc-normalization root cause needs raw to re-fix.) |
| DATA-15 | MED | ✅ FIXED | Added `capture24` `DATASET_CONFIGS` block + global de-underscore fallback in `label_augmentation.py`. |
| DATA-16 | LOW | 🔶 PARTIAL | Gravity-detector hardening reduces the dual-acc misfire; dropping the redundant uci_har `total_acc` triad (layout root cause) deferred. |
| DATA-17 | LOW | 🔶 PARTIAL | "at N Hz" stripped corpus-wide; the explicit "(units, includes gravity)" annotation per manifest is partly done (kuhar) — remaining datasets deferred, but gravity detection now reads documented text. |
| DATA-18 | LOW | ✅ FIXED | pamap2 `main()` now `random.seed(42)` once + clears `sessions/` before writing (code-only; converter **not** re-run, committed data untouched). |
| DATA-19 | LOW | ✅ FIXED+VERIFIED | `get_channel_counts` now reads each session's actual parquet columns — wisdm phantom 12-ch bucket gone. |
| DATA-20 | LOW | ✅ FIXED | dsads Hz literal stripped (part of the 13-manifest "at N Hz" strip). |
| DATA-21 | LOW | ✅ FIXED | `_gravity_present` absolute-magnitude requirement added → gravity-removed kuhar/uci_har body_acc correctly skip P1/P2. |
| DATA-22 | LOW | ✅ FIXED | unimib_shar "at 50Hz" literal stripped. |
| DATA-23 | LOW | 🚩 FLAGGED | mhealth gyro ~14 Hz effective rate noted in channel text (paired with DATA-13). Per-channel true-rate persistence deferred. |
| DATA-24 | LOW | 🔶 CODE-FIXED (raw absent) | recgym converter now groups by `Session` (third key) so windows don't straddle recording boundaries; per-session timestamps reset. Re-run blocked on raw. |
| DATA-25 | LOW | 🔶 CODE-FIXED (raw absent) | recgym subject IDs + windowing seeds now deterministic (`zlib.crc32`, not salted `hash()`). Re-run blocked on raw. |
| DATA-26 | LOW | ✅ FIXED | capture24 "at 100 Hz" literal stripped. |

### MODELS (23)
| # | Sev | Status | Note |
|---|---|---|---|
| MODELS-1 | HIGH | 🔶 PARTIAL / ⏳ DEFERRED | `assemble_v2_table.py` now annotates partial averages (`n/N`, ⚠) so a 1-dataset avg can't masquerade as full — closes the *misleading-table* half now. Actual re-run of baselines + doc==artifact regeneration is **Phase-4** (post-retrain). |
| MODELS-2 | MED | ⏳ DEFERRED (Phase-4) | LiMU-BERT row regeneration needs a clean 6-dataset run + re-score. |
| MODELS-3 | MED | ⏳ DEFERRED (re-pretrain) | CrossHAR must be re-pretrained on the 11-dataset corpus (incl. capture24) for parity. |
| MODELS-4 | MED | ✅ FIXED | Legacy `evaluate_crosshar.py` / `evaluate_limubert.py` `__main__` gated behind `TSFM_ALLOW_LEGACY_*` env flags (warn + `SystemExit(1)` otherwise) — v1 group-scored / subject-leaking numbers can no longer be emitted. |
| MODELS-5 | MED | ✅ FIXED | `baseline_flexibility.md` corrected: UniMTS released checkpoint is **accel-only (3ch)**. |
| MODELS-6 | MED | ✅ FIXED (doc) | Documented UniMTS per-dataset unit map requirement (m/s² accel, rad/s gyro; non-uniform). Adapter enforcement at build. |
| MODELS-7 | MED | 🔶 PARTIAL | Cosine-tier label-normalization rule captured; eval de-underscores labels. Symmetric HALO-side normalization + adapter clause finalized at build. |
| MODELS-8 | MED | ✅ FIXED | UniMTS window handling corrected to "fixed 10 s (first-200 @ 20 Hz) + wrap-pad", reclassified under "fixed". |
| MODELS-9 | MED | ✅ FIXED | NormWear sampling-rate cell → "fixed 65 Hz (resample, lossy)". |
| MODELS-10 | MED | ✅ FIXED (doc) | Documented NormWear's 65 Hz pre-resample + full preproc requirement (resample only auto-triggers >256 Hz). `preprocess_normwear.py` built with adapter. |
| MODELS-11 | MED | ✅ FIXED (doc) | Documented NormWear is **not** plain cosine (MSiTF checkpoint, query-conditioned fusion, Clinical-TinyLlama labels, L1 scoring). Bespoke adapter at build. |
| MODELS-12 | MED | ⏳ DEFERRED (doc at build) | Out-of-domain (physiological FM on IMU) + ~385 h distinct (not 14,943 h) disclosure to land in RESULTS_V2 when the head-to-head runs. |
| MODELS-13 | MED | ⏳ DEFERRED (build-time) | ssl-wearables g-units/gravity-present/30 Hz/3-ch input requirement documented in the matrix; enforced when the adapter is built (from raw, not the m/s² limubert copy). |
| MODELS-14 | MED | ⏳ DEFERRED (doc at build) | UK-Biobank pretraining-corpus asymmetry disclosure at build. |
| MODELS-15 | MED | ⏳ DEFERRED (build-time) | DeepConvLSTM from-scratch recipe to be pinned in the protocol when the adapter is built (reuse pre-registered split/subsample; report full-shot). |
| MODELS-16 | MED | ⏳ DEFERRED (build-time) | DeepConvLSTM input format to be pre-registered at build. |
| MODELS-17 | MED | ⏳ DEFERRED (Phase-4 + user) | opportunity→inclusivehar staleness in RESULTS_V2/PROTOCOL/CLAUDE.md is a **benchmark-composition decision** entangled with the re-score — flagged for user confirmation, not unilaterally rewritten. |
| MODELS-18 | MED | ✅ FIXED | `assemble_v2_table.py` renders partial rows as `n/N` + ⚠, never an unannotated partial avg beside a full one. |
| MODELS-19 | MED | ✅ FIXED | Trainable-params column added (HALO ~25.8M active, ConSE baselines ~62.6K). RESULTS_V2 "input-parity not capacity-parity" reword lands with the Phase-4 doc regen. |
| MODELS-20 | LOW | ⏳ DEFERRED (doc at build) | Verified-clean contamination check to be recorded in the NormWear adapter-build note. |
| MODELS-21 | LOW | ⏳ DEFERRED (build-time) | ssl-wearables freeze-trunk/head-only parity rule to be documented + enforced at build. |
| MODELS-22 | LOW | ⏳ DEFERRED (doc at build) | wrist-only / gravity-present OOD handicap disclosure at build. |
| MODELS-23 | LOW | ✅ FIXED | macro-F1 cross-model comparability caveat added to `EVALUATION_PROTOCOL_V2.md` (Metrics). Per-cell `n_scored_classes` surfacing lands with the Phase-4 table regen. |

### EVAL (2)
| # | Sev | Status | Note |
|---|---|---|---|
| EVAL-1 | MED | ✅ FIXED | Parity-row claim reworded: matched **nominal** 20 Hz + neutral text, each model in its own required normalization/anti-aliasing (byte-identical signal is impossible across architectures). |
| EVAL-2 | MED | ✅ FIXED | `assemble_v2_table.py` no longer prints averages over different supports unannotated (partial `n/N` + ⚠). |

### Tally
- ✅ FIXED (verified where possible): **28** — every protocol-survivable data/eval bug that has local data or is code/config/doc-only.
- 🔶 CODE-FIXED, raw absent (recgym×2, kuhar rate) + 🔶 PARTIAL (uci_har dual-acc, hhar/units, MODELS-1/7): **6**.
- 🚩 FLAGGED honestly, no fabrication (mhealth gyro ×2, kuhar 1101): **3**.
- ⏳ DEFERRED to build-time (baseline adapters not yet built) or Phase-4 (post-retrain re-score / doc==artifact regen): **14** — none is a bug a retrain wouldn't subsume; all are "score/document after the model exists".

**Blocked-on-raw note:** re-running the recgym / kuhar / mhealth converters requires re-fetching their raw sources (`datascripts/<ds>/download.py`). Their converter code is now correct + deterministic; the on-disk data remains from the prior converter but is usable (true timestamps, correct labels; the only honestly-degraded block is mhealth gyro, which is flagged in its channel text).
