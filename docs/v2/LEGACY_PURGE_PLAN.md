# V2 legacy purge — verified removal plan (audit wf_61ab26ea-771)

_14 agents, 7 areas, adversarially verified. Safety tag: v2-pre-legacy-purge (541b68c)._
_~2,577 LOC across 46 removals. Coupled refactor: config+call-sites must land atomically with class deletions._

## tokenizer  (verify=SOME_UNSAFE)
- **MultiScaleConv1D** `model/feature_extractor.py:76-144` [safe] ~68LOC
  - why dead: Conv building block used ONLY internally by ChannelIndependentCNN and SpectralTemporalExtractor (both removed here). The V2 PhysicalFilterbankTokenizer has no convolutions (pure rDFT + Gaussian filterbank + one Linear).
  - depends_on: ['ChannelIndependentCNN', 'SpectralTemporalExtractor']
- **ChannelIndependentCNN** `model/feature_extractor.py:146-285` [safe] ~140LOC
  - why dead: Instantiated only inside FixedPatchCNN.__init__ (self.cnn). FixedPatchCNN is the removed 'cnn' extractor path.
  - depends_on: ['FixedPatchCNN']
- **SpectralTemporalExtractor** `model/feature_extractor.py:288-435` [strands-shipped-ckpt] ~148LOC
  - why dead: Selected ONLY by encoder branch feature_extractor_type=='spectral_temporal' (model/encoder.py:161-170). V2-live contract: PhysicalFilterbankTokenizer supersedes it. No live config sets spectral_temporal after the config flip (all current presets that do are to be switched to physical_filterbank).
  - depends_on: ['encoder spectral_temporal dispatch branch']
- **FixedPatchCNN** `model/feature_extractor.py:438-500` [strands-shipped-ckpt] ~63LOC
  - why dead: Selected by the encoder default/else branch (model/encoder.py:171-178) for feature_extractor_type not in {physical_filterbank, spectral_temporal} (encoder default is 'cnn'). Superseded by PhysicalFilterbankTokenizer.
  - depends_on: ['encoder cnn/else dispatch branch']
- **test_feature_extractor + __main__ block** `model/feature_extractor.py:869-955` [touches-tests] ~87LOC
  - why dead: Module self-test that instantiates and asserts ONLY FixedPatchCNN; becomes unrunnable once FixedPatchCNN is removed. It never touches PhysicalFilterbankTokenizer.
  - depends_on: ['FixedPatchCNN']
- **legacy tokenizer imports (FixedPatchCNN, SpectralTemporalExtractor)** `model/encoder.py:17,23` [needs-config-update] ~2LOC
  - why dead: Both import lines (try: .feature_extractor @17; except script @23) pull FixedPatchCNN + SpectralTemporalExtractor + PhysicalFilterbankTokenizer; after removals only PhysicalFilterbankTokenizer survives. Trim to `from .feature_extractor import PhysicalFilterbankTokenizer`.
  - depends_on: ['SpectralTemporalExtractor', 'FixedPatchCNN', 'encoder spectral_temporal dispatch branch', 'encoder cnn/else dispatch branch']
- **encoder feature-extractor dispatch (spectral_temporal elif + cnn else)** `model/encoder.py:161-178` [strands-shipped-ckpt] ~18LOC
  - why dead: V2-live is filterbank-only. Collapse to the `if self.is_filterbank:` arm (make PhysicalFilterbankTokenizer unconditional, or raise ValueError for any non-physical_filterbank type). The elif builds SpectralTemporalExtractor, the else builds FixedPatchCNN.
  - depends_on: ['SpectralTemporalExtractor', 'FixedPatchCNN', 'config presets feature_extractor_type flip', 'preprocessing interpolate/zscore branch (out-of-scope agent)']
- **torch.compile feature-extractor dispatch (hasattr(fe,'cnn') / SpectralTemporal else)** `training_scripts/human_activity_recognition/semantic_alignment_train.py:2489-2504` [needs-config-update] ~16LOC
  - why dead: Both arms compile removed extractors: the if-arm indexes fe.cnn.layers/fe.cnn.projection (FixedPatchCNN), the else-arm indexes fe.temporal_layers/temporal_proj/spectral_mlp (SpectralTemporalExtractor). PhysicalFilterbankTokenizer has NEITHER, so with the filterbank live this block's else-arm would AttributeError on fe.temporal_layers. Must be replaced with compiling fe.proj (the tokenizer's only Linear) or skipping FE compile.
  - depends_on: ['FixedPatchCNN', 'SpectralTemporalExtractor']
- **grad-norm component keys fe_temporal / fe_spectral** `training_scripts/human_activity_recognition/semantic_alignment_train.py:914-916` [safe] ~3LOC
  - why dead: Diagnostic grad-norm tracker maps 'fe_temporal'->encoder.feature_extractor.temporal and 'fe_spectral'->encoder.feature_extractor.spectral, which are SpectralTemporalExtractor prefixes. On the filterbank these prefixes match nothing (silent 0 norms, no crash). Update to encoder.feature_extractor.proj or drop the two keys.
  - depends_on: ['SpectralTemporalExtractor']
  KEEP/uncertain:
  - PhysicalFilterbankTokenizer: The surviving V2 tokenizer (model/feature_extractor.py:503-866) + FB_* default constants (31-72). KEEP entirely. Its .proj Linear is the only submodule the training-script compile block should target 
  - IMUActivityRecognitionEncoder.target_patch_size / interpolation_method / normalization_method (+ preprocessing interpolate/zscore branch): These belong to the PREPROCESSING subsystem, not the tokenizer. The encoder cnn/spectral dispatch removal DEPENDS on preprocessing being switched to filterbank-only (pad_to_size=dft_size). Do NOT dele
  - MODEL_SIZE presets TINY/SMALL/SMALL_DEEP/MEDIUM/LARGE (non-_fb): Size machinery is LIVE (HALO-Base incoming) so KEEP the presets, but every one currently pins feature_extractor_type to cnn/spectral_temporal — a removed path. Main agent must decide: flip each preset
  - model_loading.py feature_extractor_type/dft_size fallbacks (lines 44,101,181 + dft_size 51,187): These default feature_extractor_type to 'cnn' and dft_size to 512. After the CNN/spectral encoder branches are gone, loading the shipped headline checkpoint (small_deep_v2_4b3fdd6, spectral_temporal) 
  CONFIG updates:
  - model/config.py:38 TINY_CONFIG feature_extractor_type 'spectral_temporal' -> 'physical_filterbank' (+ add n_bands/f_min/f_max/tokenizer_Q/dft_size/nyquist_margin/tokenizer_norm/use_amplitude/use_dc/use_resolution_mask; drop spectral_ratio@39)
  - model/config.py:93 SMALL_CONFIG feature_extractor_type 'cnn' -> 'physical_filterbank' (drop spectral_ratio@94)
  - model/config.py:143 SMALL_DEEP_CONFIG feature_extractor_type 'spectral_temporal' -> 'physical_filterbank' (drop spectral_ratio@144) — or retire SMALL_DEEP in favor of SMALL_DEEP_FB
  - model/config.py:233 MEDIUM_CONFIG feature_extractor_type 'spectral_temporal' -> 'physical_filterbank' (drop spectral_ratio@234)
  - model/config.py:288 LARGE_CONFIG feature_extractor_type 'spectral_temporal' -> 'physical_filterbank' (drop spectral_ratio@289)
  - model/encoder.py:80 default feature_extractor_type='cnn' -> 'physical_filterbank'; remove now-dead spectral_ratio param@81 and its docstring lines 127-128
  - model/encoder.py:88 dft_size default 512 -> align with config (SMALL_DEEP_FB uses 256)
  - val_scripts/human_activity_recognition/model_loading.py:44,101,181 feature_extractor_type fallback 'cnn' -> 'physical_filterbank'; dft_size fallback @51,187 512 -> 256 to match config
  - training_scripts/human_activity_recognition/semantic_alignment_train.py:202 FEATURE_EXTRACTOR_TYPE default 'cnn' and :1873 saved_cfg fallback 'cnn' -> 'physical_filterbank'
  - model/feature_extractor.py:3-5 module docstring ('Fixed 1D CNN architecture...') is stale once the CNN classes go — rewrite to describe PhysicalFilterbankTokenizer only
  - model/__init__.py / model/encoder.py:1-8 docstrings describe interpolate-to-64 + z-score + 1D CNN flow — stale, update to filterbank flow
  TEST updates:
  - tests/test_encoder_forward.py — ENTIRE FILE depends on the removed CNN path: fixtures build IMUActivityRecognitionEncoder with default feature_extractor_type ('cnn') and feed 64-len patches (encoder@32-41, gradient test@93-99; classes TestEncoderForwardPass/TestSemanticHeadOutput/TestMaskBehavior). Rewrite to feature_extractor_type='physical_filterbank' with dft_size-padded patches + sampling_rate_hz + patch_len_samples, or the file breaks at encoder construction/forward.
  - tests/test_model_loading.py — encoder_and_head fixture (@34-40) builds encoder with default cnn extractor and TestModelConstruction feeds 64-len patches (@56-68). Update to filterbank construction, or these break.
  - model/feature_extractor.py test_feature_extractor() (@869-955) — remove with FixedPatchCNN (self-test of the CNN path).
  - tests/test_physical_filterbank_tokenizer.py — KEEP; this is the surviving tokenizer test (imports PhysicalFilterbankTokenizer @24, encoder FB-mode test @347-360 uses feature_extractor_type='physical_filterbank'). No change required; it should be the reference for rewriting the two tests above.
  ⚠ UNSAFE (coupling required):
  - encoder.__init__ spectral_ratio param removal (model/encoder.py:81) — listed under config_updates as "remove now-dead spectral_ratio param@81": Two LIVE v2 paths still pass spectral_ratio= as a keyword arg to IMUActivityRecognitionEncoder and are NOT in the plan's update list, so dropping the param raises TypeError on both: (a) LIVE filterbank TRAINING — training_scripts/human_activity_recognition/semantic_alignment_train.py:2092 `spectral_ratio=SPECTRAL_RATIO,` inside the encoder construction that also passes n_bands/dft_size/etc.; (b) LIVE filterbank EVAL-LOAD — val_scripts/human_activity_recognition/model_loading.py:182 `spectral_ratio=enc_cfg.get('spectral_ratio', 0.25),` in the encoder hydration used to load ANY checkpoint including filterbank ones. The plan's model_loading config_updates only flip the feature_extractor_type and dft_size fallbacks; it does not remove the spectral_ratio kwarg at model_loading.py:182 or train:2092. Safe fix: either keep spectral_ratio as an accepted-but-ignored kwarg, or delete the kwarg at BOTH 2092 and 182 (and SPECTRAL_RATIO at train:203/1874/1994/2026) in the same commit.
  + verifier-found extra:
  - encoder params cnn_channels / cnn_kernel_sizes / patch_chunk_size (model/encoder.py:61-63) become dead once BOTH FixedPatchCNN and SpectralTemporalExtractor are removed — they are consumed only by those two extractors. The plan's keep_uncertain covers target_patch_size/interpolation/normalization but omits these three. Corresponding live pass-sites CNN_CHANNELS/CNN_KERNEL_SIZES at train:2089 and model_loading.py:177-178 must be handled if the params are dropped (same TypeError risk as spectral_ratio).
  - grad-norm fe_temporal/fe_spectral keys appear at MORE sites than the plan's 914-916: init dicts at semantic_alignment_train.py:971 and :1225 ('fe_temporal':0.0,'fe_spectral':0.0), and the component-name list at :2583 (['...','fe_temporal','fe_spectral']). All reference SpectralTemporalExtractor prefixes; harmless (silent 0) but should be cleaned with 914-916.
  - SPECTRAL_RATIO module variable and its persistence: train:203 (_cfg.get), :1874 (saved_cfg restore), :1994 and :2026 (written into saved hyperparameters json). Vestigial after spectral removal; the :2092 kwarg use is the actively-unsafe one (see unsafe_removals).
  - model/README.md:220 documents spectral_ratio + feature_extractor_type='spectral_temporal' — stale docs in this area, not in the plan's docstring-update list (which covers feature_extractor.py header + encoder.py docstrings only).

## preprocessing  (verify=ALL_SAFE)
- **interpolate_patches** `model/preprocessing.py:66-118` [strands-shipped-ckpt] ~53LOC
  - why dead: Resamples native-rate patches to a fixed target_patch_size for the CNN/spectral-temporal legacy path. In v2-live (feature_extractor_type=physical_filterbank) the tokenizer consumes native-rate zero-padded patches, so preprocess_imu_data is always called with pad_to_size=dft_size (non-None) and takes the filterbank branch (create_patches -> zero_pad_patches) that NEVER calls interpolate_patches. It is only reached via the legacy branch at preprocessing.py:274 when pad_to_size is None, i.e. for cnn/spectral_temporal encoders that v2 supersedes.
  - depends_on: ['Removal of legacy encoder feature_extractor path (cnn/spectral_temporal) so preprocess_imu_data is never called with pad_to_size=None', 'Removal C (legacy branch of preprocess_imu_data) must land in the same change since it is the sole caller']
- **normalize_patches** `model/preprocessing.py:151-216` [strands-shipped-ckpt] ~66LOC
  - why dead: Per-patch per-channel z-score/minmax normalization used by the legacy interpolate+normalize path. The V2 contract explicitly states the filterbank preprocessing does NO per-patch z-score (the PhysicalFilterbankTokenizer does its own signed-DC removal + amplitude preservation). Filterbank branch returns means=None/stds=None and never normalizes. Only reached via legacy branch at preprocessing.py:275 (pad_to_size=None).
  - depends_on: ['Removal of legacy encoder feature_extractor path (cnn/spectral_temporal)', 'Removal C (legacy branch) since it is the sole caller']
- **preprocess_imu_data legacy branch (interpolate->normalize) + legacy-only params (target_patch_size, normalization_method, interpolation_method)** `model/preprocessing.py:273-287 (branch); 224-226 (dead signature params after branch removal)` [strands-shipped-ckpt] ~20LOC
  - why dead: Lines 273-287 are the `pad_to_size is None` fall-through that calls interpolate_patches + normalize_patches and builds the legacy metadata dict. In v2-live the filterbank branch (lines 258-271) always fires because callers pass pad_to_size=dft_size for physical_filterbank models. Once the legacy encoder path is gone, no caller ever passes pad_to_size=None, so the branch and its exclusive params (target_patch_size, normalization_method, interpolation_method) are unreachable. After removal, preprocess_imu_data becomes filterbank-only and pad_to_size can be made mandatory. create_patches (255) and zero_pad_patches stay.
  - depends_on: ['Removal of legacy encoder feature_extractor path (cnn/spectral_temporal) in model/encoder.py + model/feature_extractor.py', 'Update encoder.py:232-241 to stop passing target_patch_size/normalization_method/interpolation_method and always pass pad_to_size=dft_size', 'Update multi_dataset_loader.py:576-592 to stop passing target_patch_size (filterbank-only)', 'Removal of legacy MODEL_SIZE presets / feature_extractor_type in {cnn,spectral_temporal} in model/config.py']
  KEEP/uncertain:
  - create_patches (model/preprocessing.py:12-63): LIVE. Used by BOTH branches: preprocess_imu_data:255 calls it unconditionally, and the filterbank path (create_patches -> zero_pad_patches) depends on it. Do NOT remove.
  - zero_pad_patches (model/preprocessing.py:121-148): LIVE v2 filterbank primitive. Called at preprocess_imu_data:260 in the filterbank branch to zero-pad native patches to dft_size with NO resampling. Core to PhysicalFilterbankTokenizer preprocessing. K
  - preprocess_imu_data (model/preprocessing.py:219-...) the function itself: KEEP the function and its filterbank branch (lines 258-271); only strip the legacy branch + legacy params. It is the single preprocessing entrypoint for both the encoder (encoder.py:232) and the datal
  - target_patch_size parameter (preprocess_imu_data signature + encoder/loader plumbing): Inside preprocessing.py it is legacy-only (used solely at line 274). BUT target_patch_size also flows into the legacy encoder/feature_extractor (CNN kernel sizing) and the loader collate (multi_datase
  CONFIG updates:
  - model/config.py: `normalization_method` ("zscore") in TINY/SMALL/SMALL_DEEP/MEDIUM/LARGE presets (config.py:40,95,145,235,290) only feeds the removed normalize_patches path -> drop once legacy branch removed (config-area agent).
  - model/config.py: `interpolation_method` ("linear") in the same legacy presets (config.py:41,96,146,236,291) only feeds the removed interpolate_patches path -> drop.
  - model/config.py: `feature_extractor_type` in {"cnn","spectral_temporal"} presets are the configs that keep the legacy preprocessing branch reachable; keeping small_deep_fb (physical_filterbank, config.py:188) is what makes the legacy preprocessing branch fully dead. (config-area, listed here to show the coupling.)
  + verifier-found extra:
  - Not a missed preprocessing symbol, but a test-coupling the plan under-flags: tests/test_encoder_forward.py:38 constructs a NON-filterbank encoder (target_patch_size=64, no feature_extractor_type => default cnn) and asserts forward-pass shapes. It does NOT call preprocess so it survives the preprocessing removals, but it WILL break when the ENCODER-area removal deletes the non-filterbank forward branch. Flag it for update in the encoder purge, not deletion.
  - model_loading.py:44/101/181 default feature_extractor_type='cnn' and :187 dft_size default 512; :179 passes target_patch_size to the encoder ctor. These keep the legacy encoder constructible during eval and are the concrete sites that 'strand the shipped checkpoint' once the legacy path/preprocessing legacy branch is gone. The plan flags the risk generically but does not enumerate model_loading.py — worth listing in the coordinated encoder/config purge so the spectral checkpoint's break is intentional.
  - docs/ references (docs/v2/design_tokenizer.md:89, REDESIGN_PLAN.md:61, FEATURE_EXTRACTOR_ANALYSIS.md:17, design_application.md:131) still describe interpolate_patches/normalize_patches as present; these are prescriptive-to-delete already, so no action needed, but noting for doc-hygiene.

## semantic-head  (verify=ALL_SAFE)
- **TemporalAttention (class)** `model/semantic_alignment.py:179-234` [needs-config-update] ~56LOC
  - why dead: Only instantiated at semantic_alignment.py:402 inside the `if not per_patch_prediction:` constructor branch. Bypassed in forward at :457 whenever per_patch_prediction=True. Every live MODEL_SIZE preset that trains the V2 head is per_patch=True: TINY(config.py:73), SMALL_DEEP(:175), SMALL_DEEP_FB(inherits via **SMALL_DEEP_CONFIG at :187), MEDIUM(:268), LARGE(:323). The headline small_deep_v2 checkpoint is per_patch=True, so temporal_attention is constructed-but-never-executed there.
  - depends_on: ['Remove constructor branch model/semantic_alignment.py:397-416', 'Add per_patch_prediction:True to SMALL_CONFIG (config.py:82-122)']
- **MultiQueryPooling (class)** `model/semantic_alignment.py:237-277` [needs-config-update] ~41LOC
  - why dead: Thin wrapper over MultiQueryAttention, only instantiated at semantic_alignment.py:409 inside the `if not per_patch_prediction:` branch and only invoked at forward :465 (session-level else). Same per_patch=True proof as TemporalAttention: no live config reaches this branch. NOTE: the underlying MultiQueryAttention is NOT dead (CrossChannelFusion uses it at :135) — only this wrapper is.
  - depends_on: ['Remove constructor branch model/semantic_alignment.py:397-416', 'Add per_patch_prediction:True to SMALL_CONFIG (config.py:82-122)']
- **SemanticAlignmentHead.__init__ `if not per_patch_prediction:` construction block (self.temporal_attention + self.attention_pooling)** `model/semantic_alignment.py:397-416` [needs-config-update] ~20LOC
  - why dead: Guarded by `if not per_patch_prediction:` (:401). Constructor default is per_patch_prediction=False (:364), but every live preset overrides to True (see config.py:73/175/inherited-187/268/323); the ONLY preset that leaves it False is SMALL_CONFIG (config.py:82-122 omits the key -> train script default False at semantic_alignment_train.py:253). Fixing SMALL_CONFIG to per_patch=True makes this branch unreachable from all live configs.
  - depends_on: ['Add per_patch_prediction:True to SMALL_CONFIG (config.py:82-122)']
- **SemanticAlignmentHead.forward session-level `else:` branch (temporal_attention -> attention_pooling -> projection)** `model/semantic_alignment.py:462-467` [needs-config-update] ~6LOC
  - why dead: Executed only when self.per_patch_prediction is False; bypassed at :457 for all live (per_patch=True) configs. After removal the `if self.per_patch_prediction:` at :457 collapses to an unconditional per-patch path (the per-patch body at :458-461 is retained). The patch_mask validation at :443-451 is retained (still used to guard empty samples).
  - depends_on: ['Update tests asserting (B,384) session-level shape']
  KEEP/uncertain:
  - MultiQueryAttention (model/semantic_alignment.py:16-108): LIVE — core building block used by CrossChannelFusion.attention (:135), the per-patch V2 fusion path. Keep.
  - CrossChannelFusion (model/semantic_alignment.py:111-176): LIVE — sole channel->patch fusion in the per_patch=True path, called at forward :455. Keep (per V2-live contract).
  - ProjectionHead (model/semantic_alignment.py:280-340): LIVE — final per-patch projection at forward :460; also directly tested (test_encoder_forward.py:201-215). Keep.
  - SemanticAlignmentHead.per_patch_prediction flag + self.per_patch_prediction attribute: KEEP the flag/attribute even after removing the session-level path: it is READ at semantic_alignment_train.py:723,732 and val_scripts/.../eval_common.py:259 to select per-patch handling. Recommend for
  - model_loading.py benign_patterns 'semantic_head.temporal_attention' / 'semantic_head.attention_pooling' (val_scripts/human_activity_recognition/model_loading.py:250-252): KEEP — every existing checkpoint (headline small_deep_v2 has 48 temporal_attention + 17 attention_pooling keys; ablations likewise) still carries these weights as UNEXPECTED keys after the classes are
  - training_output/semantic_alignment/small_v1_best_20260217/best.pt: STRANDED by this removal: it is a per_patch=False `small`-preset checkpoint that ACTIVELY uses the session head (24 temporal_attention + 17 attention_pooling keys, executed at runtime). After removal 
  CONFIG updates:
  - model/config.py SMALL_CONFIG (lines 82-122): ADD "per_patch_prediction": True. REQUIRED — this is the ONLY preset that omits the key, so it currently defaults False (via semantic_alignment_train.py:253) and would construct/execute the removed session-level path -> crash. All other presets are already True (tiny:73, small_deep:175, small_deep_fb inherits at 187, medium:268, large:323).
  - FOLLOW-UP (optional, cascading — mark needs-config-update): num_semantic_temporal_layers, num_pool_queries, use_pool_self_attention become dead head config keys (they feed ONLY TemporalAttention/MultiQueryPooling). Present in every preset (e.g. small:109/112/113, small_deep:160/163/164) and read into train globals at semantic_alignment_train.py:230/233/234, re-saved at :2009/2011/2035/2037, and mapped in model_loading.py head_cfg:203(num_temporal_layers)/209/210. Removing them also means dropping the num_temporal_layers/num_pool_queries/use_pool_self_attention params from SemanticAlignmentHead.__init__ and its 4 call sites (train:2124-2127, model_loading:203-210, benchmark_scaled_model.py:66-69, tests). Per the 'do not gut the size-config system' caution, safest to leave these keys as harmless-but-unused unless a follow-up pass removes them together with the constructor params.
  - num_fusion_queries / use_fusion_self_attention: KEEP — these feed CrossChannelFusion (live).
  TEST updates:
  - tests/test_model_loading.py: `encoder_and_head` fixture (:41-49) constructs SemanticAlignmentHead WITHOUT per_patch_prediction -> defaults False -> session-level head. Add per_patch_prediction=True. Then update shape asserts that assume session-level (B,384): test_semantic_head_output_shape (:63-69, expects (2,384)) and test_model_produces_output_after_state_load (:181-195, expects (1,384)) must become (B,P,output_dim); test_embedding_is_l2_normalized (:71-78) and test_strict_false_handles_extra_keys (:171-179, encoder-only) also use the fixture.
  - tests/test_encoder_forward.py: `head` fixture (:45-50) constructs head without per_patch_prediction -> default False. Add per_patch_prediction=True. Update test_output_shape_384 (:105-110, expects (2,384) -> (2,P,384)) and the head use at :116. ProjectionHead tests (:201-215) are independent — no change.
  - scripts/benchmark_scaled_model.py:64-70 constructs the head without per_patch_prediction (default False, session-level) and passes num_pool_queries/use_pool_self_attention. Update to per_patch_prediction=True and drop the pool args when/if the constructor signature is trimmed; this is a dev benchmark, non-critical but references the removed path.
  - training_scripts/human_activity_recognition/semantic_alignment_train.py grad-norm logging references 'temporal_attention'/'attention_pooling' component names: docstring (:894-895), components dict (:911-912), default-0.0 fallback dicts (:968-969, :1222-1223), and comp_name list (:2583). NON-CRASHING (they simply log 0.0 once the submodules are gone) but should be pruned for cleanliness. Not a test file but must be updated alongside removal.
  + verifier-found extra:
  - dim_feedforward constructor param of SemanticAlignmentHead (model/semantic_alignment.py:359) becomes dead-but-harmless once the session-level branch is removed: it feeds ONLY TemporalAttention (passed at :406 as dim_feedforward=dim_feedforward) and nothing else in the head. The plan enumerated num_temporal_layers/num_pool_queries/use_pool_self_attention as the follow-up dead head params but OMITTED dim_feedforward, which is the same class of dead param. Keep-as-unused is safe (model_loading.py:205 passes dim_feedforward=head_cfg['d_model_fused']*4, benchmark_scaled_model.py:67, and both test fixtures pass it), but any follow-up that trims the constructor signature must drop dim_feedforward together with the other three (and update model_loading:205 + benchmark:67 + tests).
  - Cosmetic-only (not a code path, but for completeness): the grad-norm components dict keys 'temporal_attention'/'attention_pooling' at semantic_alignment_train.py:911-912 remain in the components map; after removal they always resolve to 0.0 (verified: compute_component_grad_norms at :921-932 sets grad_norms[name] for EVERY key in the dict regardless of whether the submodule exists, so :2584 train_metrics['temporal_attention_grad_norm'] stays populated = 0.0 and does NOT KeyError). The plan already flags these as non-crashing; confirming there is no hidden crash.

## label-bank  (verify=SOME_UNSAFE)
- **LabelAttentionPooling** `model/token_text_encoder.py:124-213` [strands-shipped-ckpt] ~90LOC
  - why dead: Learnable single-prototype attention pool. Instantiated ONLY in LearnableLabelBank.__init__ at token_text_encoder.py:487, inside the `else` (use_mean_pooling=False AND num_prototypes<=1) branch. V2 contract forces use_mean_pooling=True (encode() then routes to _mean_pool, 510-511), so this branch never executes and the class is unreachable in the live path.
  - depends_on: ['LearnableLabelBank use_mean_pooling flag + learned-pool constructor branch']
- **MultiPrototypeLabelPooling** `model/token_text_encoder.py:216-320` [touches-tests] ~105LOC
  - why dead: K-prototype pool. Instantiated ONLY in LearnableLabelBank.__init__ at token_text_encoder.py:477, inside the `elif num_prototypes > 1` branch. label_bank_num_prototypes=1 in ALL 5 config.py presets (68/121/172/265/320) and num_prototypes=1 in every shipped hyperparameters.json (headline + all 15 runpod ablations). encode() therefore always returns (L,D); num_prototypes>1 is never reached.
- **LearnableLabelBank use_mean_pooling flag + learned-pool constructor branch (params num_heads/num_queries/num_prototypes/dropout/use_mean_pooling; body if/elif/else)** `model/token_text_encoder.py:452-494` [strands-shipped-ckpt] ~30LOC
  - why dead: Constructor branches to MultiPrototypeLabelPooling / LabelAttentionPooling only when use_mean_pooling=False. V2 keeps frozen mean-pool only, so self.pooling is always None and the num_heads/num_queries/num_prototypes/dropout args only ever configured the removed pooling modules. Collapse __init__ to always mean-pool (self.pooling=None, drop the flag and pooling-only kwargs).
  - depends_on: ['LabelAttentionPooling', 'MultiPrototypeLabelPooling']
- **LearnableLabelBank.encode learned-pool else branch** `model/token_text_encoder.py:510-513` [strands-shipped-ckpt] ~4LOC
  - why dead: `if self.use_mean_pooling: return _mean_pool else: return self.pooling(...)`. With mean-pool-only, the else branch (self.pooling(...)) is unreachable; collapse encode() to unconditionally return self._mean_pool(tokens, mask, normalize).
  - depends_on: ['LabelAttentionPooling']
- **test_modules() + __main__ block** `model/token_text_encoder.py:557-618` [safe] ~62LOC
  - why dead: Module self-test. ALREADY BROKEN: line 600 calls LearnableLabelEncoder() which is defined nowhere in the repo (only LearnableLabelBank exists) -> NameError on `python model/token_text_encoder.py`. Also directly constructs LabelAttentionPooling (573) being removed. No importer runs it.
- **label_bank_num_heads / label_bank_num_queries / label_bank_num_prototypes config keys** `model/config.py:68-70,119-121,170-172,263-265,318-320` [needs-config-update] ~15LOC
  - why dead: These three keys exist in all 5 MODEL_SIZE presets and are read into LABEL_BANK_NUM_HEADS/QUERIES/PROTOTYPES (semantic_alignment_train.py:248-250) purely to configure LabelAttentionPooling/MultiPrototypeLabelPooling. Once mean-pool-only, the mean-pool path ignores them entirely, so they become dead.
  - depends_on: ['LabelAttentionPooling', 'MultiPrototypeLabelPooling']
- **SemanticAlignmentLoss._forward_multi_prototype + is_multi_prototype dispatch** `training_scripts/human_activity_recognition/semantic_loss.py:211-269,372-377` [uncertain] ~60LOC
  - why dead: OTHER AREA (training loss) but becomes dead with MultiPrototypeLabelPooling gone: dispatch `is_multi_prototype = text_embeddings.dim()==3` (:372) is only True when label_bank.encode returns (L,K,D), i.e. num_prototypes>1. With K always 1, encode returns 2D and _forward_multi_prototype is never entered.
  - depends_on: ['MultiPrototypeLabelPooling']
- **multi-prototype (dim()==3) branches in eval** `val_scripts/human_activity_recognition/evaluate_tsfm_v2.py:159-162,207-208` [uncertain] ~6LOC
  - why dead: OTHER AREA (evaluator) but becomes dead: both branches guard on label_embs/text_embs.dim()==3 (multi-prototype). encode() always returns 2D after num_prototypes>1 removal, so the einsum/max-over-prototype paths are unreachable. Same for evaluation_metrics.py compute_similarity dim()==3 branch (41-43).
  - depends_on: ['MultiPrototypeLabelPooling']
  KEEP/uncertain:
  - LearnableLabelBank._mean_pool (model/token_text_encoder.py:532-542): MUST KEEP — this IS the surviving V2 path. REPO_CLEANUP_PLAN.md:47 explicitly lists it as do-NOT-delete. encode() and encode_frozen() both route here.
  - LearnableLabelBank.encode_frozen (model/token_text_encoder.py:515-530): MUST KEEP — frozen mean-pool used to build soft targets in training (semantic_alignment_train.py:994 and :1282, mb_frozen_text). Independent of the learnable-pool removal.
  - TokenTextEncoder (model/token_text_encoder.py:19-121) and ChannelTextFusion (323-440): MUST KEEP — both are LIVE V2 components (frozen SBERT token encoder + per-channel text fusion). Not part of label-pooling removal; ChannelTextFusion is constructed in SemanticAlignmentModel and shares
  - LearnableLabelBank kwargs model_name / d_model / device / text_encoder: KEEP — needed by the mean-pool path (model_name/text_encoder build the frozen SBERT; d_model is embedding_dim; device placement). Only num_heads/num_queries/num_prototypes/dropout/use_mean_pooling are
  - model_loading.load_label_bank strict load at model_loading.py:335: VERIFY-BEFORE-DELETE side effect: for the CURRENT headline ckpt (small_deep_v2_4b3fdd6) label_bank_state_dict has 11 pooling.* tensors and use_mean_pooling=False; after removal `label_bank.load_state_
  CONFIG updates:
  - model/config.py: delete keys label_bank_num_heads, label_bank_num_queries, label_bank_num_prototypes from all 5 presets (lines 68-70, 119-121, 170-172, 263-265, 318-320) — dead once mean-pool-only.
  - val_scripts/human_activity_recognition/model_loading.py: update load_label_bank (292-348) to construct LearnableLabelBank WITHOUT num_heads/num_queries/num_prototypes/use_mean_pooling kwargs (drop :320 use_mean_pooling read and :326-328/:331 kwargs); and STOP loading pooling weights — guard so a mean-pool bank never calls load_state_dict on pooling.* (else the shipped headline ckpt errors). Also drop num_heads/num_queries/num_prototypes from the label_bank dict in _load_hyperparams (:77-79 new-fmt, :119-121 legacy).
  - training_scripts/human_activity_recognition/semantic_alignment_train.py: neutralize/remove ABLATION_LEARNABLE_LABEL_BANK (:350), USE_MEAN_POOLING (:356 — force True), FREEZE_LABEL_BANK (:357 + its dead optimizer branches :2376-2382 and :2406-2413), LABEL_BANK_NUM_HEADS/QUERIES/PROTOTYPES (:248-250); drop num_heads/num_queries/num_prototypes/use_mean_pooling from the LearnableLabelBank construction (:2184-2188); drop the saved keys label_bank_num_heads/queries/prototypes (:2013-2015), use_mean_pooling/freeze_label_bank/num_prototypes (:2046-2048), and resume reads (:1905-1907, :1939).
  - scripts/benchmark_scaled_model.py:79-82: drop num_heads/num_queries/num_prototypes/dropout kwargs from LearnableLabelBank construction (signature changes when the pooling params are removed).
  TEST updates:
  - tests/test_model_loading.py: remove LabelAttentionPooling, MultiPrototypeLabelPooling from the import (lines 21-22); DELETE test_multi_prototype_encode_shape (106-119) and test_multi_prototype_embeddings_normalized (131-140); UPDATE test_single_prototype_encode_shape (92-104), test_label_bank_embeddings_normalized (121-129), test_label_bank_state_dict_roundtrip (142-165) to construct LearnableLabelBank without num_heads/num_queries/num_prototypes kwargs.
  - tests/test_encoder_forward.py: remove LabelAttentionPooling/MultiPrototypeLabelPooling from import (20-23); in TestMultiPrototypeLabelBank (158-198) DELETE test_k3_shape (170-177) and test_k3_normalized (179-187); UPDATE test_k1_shape (161-168) and test_different_labels_produce_different_embeddings (189-198) to drop the pooling kwargs.
  - tests/test_similarity_computation.py: label_bank constructions at 93-95 and 118-120 pass num_heads/num_queries/num_prototypes — update to the mean-pool-only signature.
  - tests referencing multi-prototype eval branches: none directly, but if the downstream dim()==3 branches (evaluate_tsfm_v2.py, evaluation_metrics.py, semantic_loss._forward_multi_prototype) are also removed, confirm no test asserts on (L,K,D) shapes (only the deleted test_k3_*/test_multi_prototype_* above do).
  ⚠ UNSAFE (coupling required):
  - LabelAttentionPooling (class, token_text_encoder.py:124-213) AND the LearnableLabelBank use_mean_pooling/learned-pool __init__ branch (452-494) AND encode() else branch (510-513): Reachable from the LIVE eval scripts today (contract criterion c), not just a doc reference. val_scripts/human_activity_recognition/model_loading.py load_label_bank builds LearnableLabelBank with use_mean_pooling read at :320 and passes num_heads/num_queries/num_prototypes at :326-328, then at :335 does `label_bank.load_state_dict(lb_state)` UNCONDITIONALLY whenever use_learnable_lb (ablation.learnable_label_bank defaults True). I verified by loading best.pt that label_bank_state_dict = 11 pooling.* tensors (pooling.queries, pooling.cross_attn.*, pooling.norm.*, pooling.out_proj.*) and hyperparameters.json has use_mean_pooling:false. This is true of ALL 16 checkpoints on disk (small_deep_v2_4b3fdd6 headline + P6_headline_s42/43/44 + every P1_/P7_ ablation) — NONE use mean-pool, NONE use K>1. Deleting the class + forcing mean-pool WITHOUT the atomic loader rewrite makes evaluate_tsfm_v2.py -> load_model/load_label_bank raise on the unexpected pooling.* keys and on the removed constructor kwargs, breaking every eval run against every existing checkpoint. Removal is safe ONLY if model_loading.load_label_bank is updated in lockstep (drop the pooling kwargs + guard so pooling.* weights are never load_state_dict'd). The plan does enumerate this loader update in config_updates and keep_uncertain, so the bundle is internally consistent, but the class as-written is NOT currently unreachable from the eval scripts — the removal must not land independently of the model_loading.py edit. Note: even after the loader fix, headline/ablation eval runs with an UNTRAINED mean-pool (frozen SBERT mean) and will NOT reproduce the published numbers until an M4 mean-pool retrain — a semantic stranding of all 16 checkpoints, which the user has accepted for the spectral path.
  - model/config.py keys label_bank_num_heads/num_queries/num_prototypes (co-dependency with semantic_alignment_train.py:248-250): Only unsafe if deleted without the paired train edit. semantic_alignment_train.py:248-250 reads _cfg["label_bank_num_heads"], _cfg["label_bank_num_queries"], _cfg["label_bank_num_prototypes"] via hard SUBSCRIPT (not .get). Deleting the keys from the 5 config presets while leaving :248-250 raises KeyError at import of the training script. model_loading._load_hyperparams uses .get() with defaults (:77-79,:119-121) so the loader tolerates their absence, but the trainer does not. Plan flags removing :248-250 together, so safe as a bundle only.
  + verifier-found extra:
  - val_scripts/human_activity_recognition/plot_utils.py:556-558 and :805-807 — `if text_embeddings.ndim == 3: text_embeddings = text_embeddings.mean(axis=1)` multi-prototype averaging branches become dead once MultiPrototypeLabelPooling/K>1 is removed (encode always returns 2D). Not listed in the plan; harmless figure code but genuinely dead.
  - val_scripts/human_activity_recognition/eval_v2.py ~:201 — a collapse-multi-prototype similarity helper (docstring 'Collapse multi-prototype similarity (N, L, K) -> (N, L) by max over K') becomes dead with K>1 gone; not enumerated in the plan.
  - training_scripts/human_activity_recognition/semantic_alignment_train.py no_decay_keywords set (~:2388) includes 'queries' specifically to exempt the LabelAttentionPooling.queries parameter from weight decay; becomes a no-op label-bank concern once pooling is removed (cosmetic).
  - Stale comments/docstrings referencing the removed pooling: semantic_alignment_train.py:899 ('label_pooling: LabelAttentionPooling with learnable queries') and :942 ('LearnableLabelBank with LabelAttentionPooling') — not code, but should be cleaned when the branch is removed.

## augmentation  (verify=ALL_SAFE)
- **IMUAugmentation (entire class: __init__, apply, create_positive_pair, jitter, scale, time_shift, channel_shuffle, rotation_3d, time_warp, magnitude_warp, resample)** `datasets/imu_pretraining_dataset/augmentations.py:18-556` [touches-tests] ~539LOC
  - why dead: Old batch-based (B,T,C)+aug_types augmentation API. Fully superseded by the per-sample IMUSample/IMUAugmenter Cfg pipeline (lines 559-1180). The v2-live loader instantiates IMUAugmenter (multi_dataset_loader.py:253) driven by AugmentationConfig; it never constructs IMUAugmentation. Training builds AUG_CONFIG=AugmentationConfig.default_v2() and passes it to the loader (semantic_alignment_train.py:383-406,2241). create_positive_pair/channel_shuffle/resample(method) exist ONLY on this class and are never called anywhere.
  - depends_on: ['Remove IMUAugmentation from datasets/imu_pretraining_dataset/__init__.py import (line 10) and __all__ (line 18)', 'Delete tests/test_augmentations.py classes TestRotation3D (128-228) and TestApplyAugProb (231-263) plus the IMUAugmentation import (line 16)']
- **YawRotationCfg** `datasets/imu_pretraining_dataset/augmentations.py:631-641` [touches-tests] ~11LOC
  - why dead: P2 yaw-only rotation config. Superseded by Rotation3dCfg/_rotation_3d (full SO(3) subsumes yaw). Disabled in EVERY preset: base default enabled=False (line 639), default_v2 explicitly sets cfg.yaw_rotation.enabled=False (line 783), legacy()/none() leave it off. No live config enables it; its own docstring says 'prefer the full-SO(3) rotation_3d below'.
  - depends_on: ['Remove yaw_rotation field/ORDER/default_v2 entries', 'Remove _yaw_rotation method + _rodrigues helper', 'Update/delete yaw tests']
- **yaw_rotation (AugmentationConfig field + ORDER entry + default_v2 disable line)** `datasets/imu_pretraining_dataset/augmentations.py:759, 769-771, 783` [touches-tests] ~5LOC
  - why dead: The `yaw_rotation: YawRotationCfg` field (759), its slot in ORDER (771, string 'yaw_rotation'), the ORDER comment about yaw running before gravity (769-770), and the `cfg.yaw_rotation.enabled = False` line in default_v2 (783) all exist only to wire the superseded yaw aug. Removing the field REQUIRES removing line 783 (else AttributeError) and the ORDER string (else IMUAugmenter.__call__ getattr(self,'_yaw_rotation') fails). rotation_3d is the live P2 lever.
  - depends_on: ['Remove YawRotationCfg', 'Remove _yaw_rotation + _rodrigues']
- **_yaw_rotation (method) and _rodrigues (module helper)** `datasets/imu_pretraining_dataset/augmentations.py:1041-1080 (_yaw_rotation), 893-907 (_rodrigues)` [touches-tests] ~55LOC
  - why dead: _yaw_rotation is only dispatched via ORDER's 'yaw_rotation' entry, which is disabled in all presets. _rodrigues is a rotation-about-axis helper used ONLY inside _yaw_rotation, so it dies with it. The live full-SO(3) path uses _random_so3 (a separate helper) instead.
  - depends_on: ['Remove yaw_rotation ORDER string (else dispatch KeyError)']
- **USE_ROTATION_AUGMENTATION** `training_scripts/human_activity_recognition/semantic_alignment_train.py:370` [safe] ~1LOC
  - why dead: Module-level constant `USE_ROTATION_AUGMENTATION = False  # DEPRECATED (superseded by AUG_CONFIG.yaw_rotation)`. Defined and never read; its comment references the also-removed AUG_CONFIG.yaw_rotation. Rotation is now controlled entirely via AUG_CONFIG (default_v2 -> rotation_3d).
- **use_rotation_augmentation (loader ctor param + self attr)** `datasets/imu_pretraining_dataset/multi_dataset_loader.py:183, 235` [safe] ~2LOC
  - why dead: Constructor param `use_rotation_augmentation: bool = False,  # DEPRECATED: use aug_config` (183) is stored to self.use_rotation_augmentation (235) and never read anywhere. Legacy rotation is fully replaced by aug_config/IMUAugmenter.
  KEEP/uncertain:
  - AugmentationConfig.legacy() / AugmentationConfig() base default (jitter+scale) / AugmentationConfig.none(): Names LOOK legacy but are LIVE. default() (jitter+scale) is the loader's backward-compat fallback (multi_dataset_loader.py:250-251). legacy and none are selectable training presets via TSFM_AUG_PRESET
  - TimeShiftCfg/TimeWarpCfg/MagnitudeWarpCfg + _time_shift/_time_warp/_magnitude_warp: Disabled in default_v2 (enabled=False) so they LOOK like dead-by-default, but they are distinct, independently-toggleable augs (not subsumed by any other aug the way yaw is by rotation_3d). They remai
  - from scipy import interpolate (augmentations.py:14): Sits atop the removable old class but is used by the LIVE _time_warp (986,991) and _magnitude_warp (1007). Must survive the IMUAugmentation deletion.
  - _random_so3, _gravity_present, _mark_gravity_removed, _paraphrase_channel, _CH_SYNONYMS/_CH_TEMPLATES: Helpers for the LIVE v2 pipeline (_rotation_3d/_gravity/_channel_text_phrase). Keep. (Contrast with _rodrigues, which is yaw-only and IS a removal.)
  CONFIG updates:
  - datasets/imu_pretraining_dataset/__init__.py: remove `IMUAugmentation,` from the `from .augmentations import (...)` block (line 10) and remove `'IMUAugmentation',` from __all__ (line 18). Keep IMUAugmenter/IMUSample/AugmentationConfig.
  - datasets/imu_pretraining_dataset/augmentations.py: delete YawRotationCfg (631-641); delete the `yaw_rotation` field (759); delete the 'yaw_rotation' string from the ORDER tuple (771) and drop the yaw clause from the ORDER comment (769-770); delete `cfg.yaw_rotation.enabled = False` in default_v2 (783) and simplify its preceding comment (779-782); delete _yaw_rotation (1041-1080) and _rodrigues (893-907).
  - training_scripts/human_activity_recognition/semantic_alignment_train.py: delete USE_ROTATION_AUGMENTATION (370); reword the preset comment at 376 that says 'yaw-only rotation' -> 'SO(3) rotation'.
  - datasets/imu_pretraining_dataset/multi_dataset_loader.py: drop the `use_rotation_augmentation` ctor param (183) and `self.use_rotation_augmentation = ...` (235).
  - Optional/cosmetic: update the augmentations.py module docstring (1-10) which describes the removed old Weak/Strong aug taxonomy.
  TEST updates:
  - tests/test_augmentations.py: remove `IMUAugmentation,` from the import (line 16); DELETE class TestRotation3D (128-228) and class TestApplyAugProb (231-263) entirely (both exercise the removed IMUAugmentation batch API); in test_default_v2_enables_p1_p4 (51-57) drop the `and not cfg.yaw_rotation.enabled` clause on line 57 (keep `cfg.rotation_3d.enabled`); DELETE test_yaw_preserves_norm_and_gravity_direction (77-84).
  - tests/test_augmentation_effects.py: DELETE test_yaw_rotation_preserves_gravity_and_norm (92-101); in test_default_v2_uses_full_rotation_not_yaw (141-144) drop the `and not cfg.yaw_rotation.enabled` assertion (or delete the test).
  + verifier-found extra:
  - datasets/imu_pretraining_dataset/README.md:150-162 — stale doc that still teaches the REMOVED IMUAugmentation API: `from datasets.imu_pretraining_dataset import IMUAugmentation` (:150), `aug = IMUAugmentation(...)` (:153), and `positive_pair = aug.create_positive_pair(data, attention_mask)` (:162). Not code, so it will NOT break `pytest`, but after the deletion it documents symbols that no longer exist. The plan's config_updates flags the augmentations.py module docstring as optional/cosmetic but omits this README. Recommend updating/deleting that README block in the same pass.
  - (context-only, NOT a code removal) Several docs cite the old symbols and would be stale after the purge: docs/v2/data_diversity_audit.md:44-50, docs/ARCHITECTURE.md:619, docs/v2/design_application.md:7, and docs/v2/adversarial_sweep_findings.md / full_sweep_findings_opus.md (yaw/_yaw_rotation line refs). None are imported or executed, so they do not affect removal safety, but they mention the deleted YawRotationCfg/_yaw_rotation/USE_ROTATION_AUGMENTATION and would be left dangling.

## encoder-config  (verify=ALL_SAFE)
- **cnn + spectral_temporal feature-extractor dispatch branches** `model/encoder.py:161-178` [strands-shipped-ckpt] ~18LOC
  - why dead: V2-live tokenizer is PhysicalFilterbankTokenizer (feature_extractor_type='physical_filterbank'); the only preset that sets a live value is SMALL_DEEP_FB_CONFIG (config.py:188 -> 'physical_filterbank'), which takes the is_filterbank branch. The cnn/spectral_temporal values are the superseded paths per the V2 contract.
  - depends_on: ['remove FixedPatchCNN/SpectralTemporalExtractor imports (encoder.py:17,23)', "default feature_extractor_type -> 'physical_filterbank' (encoder.py:80)", 'collapse SMALL_DEEP_FB_CONFIG into canonical small_deep + drop spectral SMALL_DEEP_CONFIG']
- **FixedPatchCNN, SpectralTemporalExtractor imports** `model/encoder.py:17,23` [needs-config-update] ~2LOC
  - why dead: Only consumed by the cnn/spectral_temporal dispatch branches (encoder.py:162,172) being removed. PhysicalFilterbankTokenizer import stays.
  - depends_on: ['remove cnn/spectral dispatch branches (encoder.py:161-178)']
- **spectral_ratio ctor param + plumbing** `model/encoder.py:81,128,168` [needs-config-update] ~3LOC
  - why dead: spectral_ratio is passed ONLY to SpectralTemporalExtractor (encoder.py:168). The filterbank branch never reads it; PhysicalFilterbankTokenizer has no such arg. Confirmed NOT read by the FB path.
  - depends_on: ['remove spectral dispatch branch (encoder.py:161-170)']
- **cnn_channels, cnn_kernel_sizes, patch_chunk_size ctor params** `model/encoder.py:61-63` [needs-config-update] ~3LOC
  - why dead: cnn_channels/cnn_kernel_sizes/patch_chunk_size are forwarded ONLY into SpectralTemporalExtractor (164-166) and FixedPatchCNN (172-177). The filterbank branch (147-160) passes none of them to PhysicalFilterbankTokenizer. Confirmed NOT read by the FB path.
  - depends_on: ['remove cnn/spectral dispatch branches (encoder.py:161-178)']
- **target_patch_size / normalization_method / interpolation_method ctor params + attrs + forward legacy assert + get_config fields** `model/encoder.py:66-68,135-137,237-239,297-300,481-483` [needs-config-update] ~12LOC
  - why dead: In the FB path these are functionally dead: (a) forward() reaches only the `if self.is_filterbank` branch which asserts seq_len==self.dft_size (283) — the else-branch assert `seq_len==self.target_patch_size` (298) is unreachable; (b) encoder.preprocess (237-239) forwards them to preprocess_imu_data, but with pad_to_size=dft_size (240) that function early-returns at preprocessing.py:271 BEFORE interpolate_patches/normalize_patches, so target_patch_size/normalization_method/interpolation_method are ignored; (c) get_config() (481-483) only echoes them. So they are still PASSED but never USED by filterbank compute.
  - depends_on: ['remove preprocessing.py legacy interpolate+zscore branch (preprocessing.py:273-287) — CROSS-AREA (preprocessing agent)', 'remove cnn/spectral dispatch + forward else-branch (encoder.py:161-178,297-300)']
- **test_encoder() self-test + __main__ block** `model/encoder.py:486-629` [touches-tests] ~144LOC
  - why dead: Module-level demo/self-test. Every call constructs IMUActivityRecognitionEncoder with the DEFAULT feature_extractor_type='cnn' and feeds 64-timestep patches (e.g. torch.randn(4,10,64,9), encode_from_raw). It exercises exactly the removed cnn path and would crash once the default becomes physical_filterbank / the cnn branch is gone.
  - depends_on: ['remove cnn/spectral dispatch branches']
- **SMALL_DEEP_CONFIG (spectral variant)** `model/config.py:132-176` [strands-shipped-ckpt] ~45LOC
  - why dead: This is the spectral_temporal (line 143) sibling of SMALL_DEEP_FB_CONFIG. V2-live headline moves to the filterbank tokenizer; SMALL_DEEP_FB_CONFIG (186-214) is the surviving small_deep. The spectral SMALL_DEEP_CONFIG only remains as the config of the to-be-stranded shipped checkpoint.
  - depends_on: ['de-spread SMALL_DEEP_FB_CONFIG so it stops inheriting from SMALL_DEEP_CONFIG (config.py:187)', "change train MODEL_SIZE default 'small_deep' -> the canonical FB config (semantic_alignment_train.py:189)"]
- **SMALL_CONFIG (cnn-only preset)** `model/config.py:82-122` [needs-config-update] ~41LOC
  - why dead: feature_extractor_type='cnn' (line 93) — the archetypal cnn-only preset the V2 contract flags. Its tier (d=384/4-layer) has no filterbank variant and no live consumer.
  - depends_on: ['update get_config map + docstring (config.py:327-354)']
  KEEP/uncertain:
  - feature_extractor_type ctor param (encoder.py:80) + the is_filterbank guard (142): KEEP. Even after cnn/spectral values are gone this is the field threaded through the save/load chain-of-custody (model_loading.py:44,101,181; train:202,1994,2026; hyperparameters.json). Safer to pin i
  - TINY_CONFIG (config.py:27-74), MEDIUM_CONFIG (222-269), LARGE_CONFIG (277-324): KEEP/CONVERT, do not delete. These are real size tiers (192/512/512 dim) and the contract says the MODEL_SIZE machinery is LIVE (HALO-Base incoming). But each currently sets feature_extractor_type='sp
  - target_patch_size / normalization_method / interpolation_method (encoder + preprocessing interface): CROSS-AREA. Removal is safe only jointly with the preprocessing legacy branch (preprocessing.py:273-287, interpolate_patches, normalize_patches) and the model_loading/train plumbing. The FB path still
  CONFIG updates:
  - model/encoder.py:80 — change feature_extractor_type default 'cnn' -> 'physical_filterbank' (or drop cnn/spectral acceptance); remove spectral_ratio(81), cnn_channels/cnn_kernel_sizes/patch_chunk_size(61-63), and (coordinated) target_patch_size/normalization_method/interpolation_method(66-68) from the ctor signature + docstring(104-131) + get_config()(476-483).
  - model/config.py — collapse SMALL_DEEP_FB_CONFIG(186-214) into the canonical SMALL_DEEP_CONFIG (rename + inline, stop `**SMALL_DEEP_CONFIG` spread at 187), delete spectral SMALL_DEEP_CONFIG(132-176) and cnn SMALL_CONFIG(82-122); drop the 6 legacy keys (feature_extractor_type when non-fb, spectral_ratio, cnn_channels, cnn_kernel_sizes, target_patch_size, normalization_method, interpolation_method) from every surviving preset; update get_config map(342-349) + docstring(327-341).
  - training_scripts/human_activity_recognition/semantic_alignment_train.py:189 — MODEL_SIZE default 'small_deep' (spectral) -> the canonical filterbank config (e.g. 'small_deep_fb' or the renamed 'small_deep').
  - training .../semantic_alignment_train.py:199-201 — CNN_CHANNELS/CNN_KERNEL_SIZES/TARGET_PATCH_SIZE read via hard `_cfg[...]` subscript; these KeyError once the keys are removed from presets. Remove the globals (and SPECTRAL_RATIO:203) or switch to .get; also update dump sites 1992-1994 & 2024-2026, ctor pass-through 2089-2092, resume readers 1870-1874 & 1920-1922, and DataLoader fallback 670.
  - training .../semantic_alignment_train.py:2490-2502 — torch.compile branch: `if hasattr(fe,'cnn')` else accesses fe.temporal_layers/temporal_proj/spectral_mlp; PhysicalFilterbankTokenizer has NONE of these (verified), so the else-branch is dead and would crash on FB. Rewrite to compile the tokenizer's actual submodules (or guard it out).
  - val_scripts/human_activity_recognition/model_loading.py — drop cnn/spectral defaults: lines 40-45, 97-102, 177-182 (cnn_channels/cnn_kernel_sizes/target_patch_size/feature_extractor_type default 'cnn'/spectral_ratio). SEPARATE PRE-EXISTING GAP to fix in the same pass: model_loading does NOT thread use_dc into the encoder (present at 46-59 & 183-195 for use_amplitude/use_resolution_mask but use_dc missing) — once FB is the only path this silently drops the signed-DC feature at load time.
  - model/preprocessing.py (CROSS-AREA, preprocessing agent) — remove the legacy interpolate+zscore branch(273-287) and interpolate_patches/normalize_patches once target_patch_size/normalization_method/interpolation_method are dropped from the encoder; preprocess_imu_data becomes filterbank-only (pad_to_size always set).
  TEST updates:
  - tests/test_encoder_forward.py — `encoder` fixture (30-41) and gradient test (91-99) build IMUActivityRecognitionEncoder with DEFAULT (cnn) extractor and feed (B,P,64,C) patches; all TestEncoderForwardPass cases (62-99) break once the default is physical_filterbank. Convert the fixture to feature_extractor_type='physical_filterbank' with dft_size patches and pass sampling_rate_hz/patch_len_samples in forward.
  - tests/test_model_loading.py — `encoder_and_head` fixture (34-40) builds a default-cnn encoder with 64-ts patches (56-67); convert to filterbank config or pin feature_extractor_type.
  - model/encoder.py:486-629 — test_encoder() module self-test uses the cnn default + 64-ts patches; delete or rewrite for filterbank (also removed as a listed item).
  - tests/test_physical_filterbank_tokenizer.py:349-352 — already builds the FB encoder (feature_extractor_type='physical_filterbank', dft_size=512); KEEP, no change beyond confirming dft_size still matches the canonical preset.
  - tests/test_data_loading.py:90-92 — uses a LOCAL var named target_patch_size in a fixture helper (not the config key); low risk, verify it doesn't assume the legacy interpolate path.
  + verifier-found extra:
  - scripts/benchmark_scaled_model.py:55-62 — constructs IMUActivityRecognitionEncoder(cnn_channels=cnn_channels, cnn_kernel_sizes=[5]) with the DEFAULT feature_extractor_type='cnn' (also line 59, driven by benchmark_config args at 220/227). This is the ONLY encoder ctor call site repo-wide the plan omits (verified via full grep of IMUActivityRecognitionEncoder( — all other sites are covered). Once cnn_channels/cnn_kernel_sizes are dropped from the ctor and the cnn branch removed, this script TypeErrors on the kwargs and has no cnn branch to hit. It is a standalone benchmark utility — NOT in the test suite, so the deletion guard will not catch it; it will be silently left broken. Add it to the config-update/removal list (delete the script or convert to physical_filterbank + FB kwargs).
  - model/__init__.py docstring (lines 1-24) still describes the removed pipeline ('Fixed patch size of 64 timesteps after interpolation', 'Per-patch, per-channel normalization', '1D CNN feature extraction', the 6-step interpolation/normalization/CNN flow). Cosmetic only (no code), but it is stale legacy documentation in this exact area the plan did not mention; update alongside the encoder.py class/ctor docstrings the plan already lists.
  - CROSS-AREA handoff (plan references but does not own): once encoder.py stops importing them, the class DEFINITIONS MultiScaleConv1D (feature_extractor.py:76), ChannelIndependentCNN (146), SpectralTemporalExtractor (288), FixedPatchCNN (438) plus the module __main__ demo (880-940) become fully dead. Confirmed no other importer via grep. Belongs to the feature_extractor agent, but flag so it is not orphaned.

## training  (verify=ALL_SAFE)
- **MemoryBank (whole MoCo-queue module)** `training_scripts/human_activity_recognition/memory_bank.py:1-181` [needs-config-update] ~181LOC
  - why dead: MoCo queue is only ever instantiated behind USE_MEMORY_BANK (env TSFM_MEMORY_BANK, default '0'). use_memory_bank=0 is the FINAL decision (repo ablation P1_none vs P1_hard_neg: +8.7pp unseen for removing the queue; docs/v2/full_sweep_findings_opus.md). No live/shipped config enables it: all 15 runpod_ablations + headline hyperparameters.json record use_memory_bank in the OFF path for the go-forward recipe, and no config preset sets it.
  - depends_on: ['USE_MEMORY_BANK machinery in train script']
- **USE_MEMORY_BANK machinery in train script (constants + all queue branches + memory_bank param threading)** `training_scripts/human_activity_recognition/semantic_alignment_train.py:38,278-279,966,996-1000,1013,1017,1088-1104,1130-1131,1168-1169,1188,2054,2199-2204,2223-2224,2453-2457,2530-2532,2581,2620` [needs-config-update] ~70LOC
  - why dead: Every site is guarded by `USE_MEMORY_BANK` (line 278, env default '0') or `memory_bank is not None` (which is None unless USE_MEMORY_BANK). Final decision use_memory_bank=0 makes all of it unreachable: line 38 import; 278-279 USE_MEMORY_BANK/MEMORY_BANK_SIZE consts; 966 total_queue_diversity init; 996-1000 get_queue_embeddings (else -> None); 1013/1017 pass imu_queue/text_queue kwargs into criterion; 1088-1104 memory_bank.update; 1130-1131 & 1168-1169 & 1188 queue_diversity metric plumbing; 2054 hyperparams logging; 2199-2204 init; 2223-2224 print; 2453-2457 resume load; 2530-2532 warmup call; 2581 epoch plot of train_metrics['queue_diversity']; 2620 save memory_bank_state_dict. Also drop the `memory_bank` parameter from _train_epoch_standard (954-955) and train_epoch (1504,1527).
  - depends_on: ['MemoryBank (whole MoCo-queue module)', 'warmup_memory_bank']
- **warmup_memory_bank** `training_scripts/human_activity_recognition/semantic_alignment_train.py:786-857` [safe] ~72LOC
  - why dead: Only caller is line 2530-2532, itself guarded by `memory_bank is not None and USE_MEMORY_BANK`. Dead once the queue is removed. (Its body also contains a per_patch=False else-branch at 843-850, dead on both counts.)
  - depends_on: ['USE_MEMORY_BANK machinery in train script']
- **compute_debug_metrics queue-diversity block** `training_scripts/human_activity_recognition/semantic_alignment_train.py:759-760,777-781` [safe] ~7LOC
  - why dead: compute_debug_metrics() itself is LIVE (called at 1045/1047/1360/1362), but the imu_queue/text_queue params are always None once the memory bank is gone, so the `if imu_queue is not None` fp32 cast (759-760) and the queue_diversity block (777-781) never execute. Drop the imu_queue/text_queue params from the signature (744) and callers.
  - depends_on: ['USE_MEMORY_BANK machinery in train script']
- **_forward_multi_prototype + multi-prototype dispatch/metrics in InfoNCELoss** `training_scripts/human_activity_recognition/semantic_loss.py:211-286,333-341,372-382` [uncertain] ~90LOC
  - why dead: Multi-prototype path fires only when text_embeddings.dim()==3 (InfoNCELoss.forward line 372 `is_multi_prototype`). With label encoding fixed to FROZEN MEAN-POOL ONLY (num_prototypes=1) the label bank always emits 2D (B,D); flatten_per_patch_embeddings keeps it 2D. All 15 ablations + headline record label_bank.num_prototypes=1 and loss_type=infonce. So _forward_multi_prototype (211-286), the is_multi_prototype branch in _compute_metrics (333-341), and the 3D dispatch arm in forward (374-377) are unreachable. Collapse forward() to always call _forward_single_prototype and drop the is_multi_prototype plumbing.
- **PRETRAINED_ENCODER_PATH + pretrained-encoder load block (Stage-1 remnant)** `training_scripts/human_activity_recognition/semantic_alignment_train.py:258,2100-2119` [safe] ~22LOC
  - why dead: PRETRAINED_ENCODER_PATH is hardcoded None (258) with no env override; the encoder trains from scratch (confirmed by __init__.py note and docs/v2 that legacy Stage-1 was removed). `if PRETRAINED_ENCODER_PATH and Path(...).exists()` (2100) is always False so the load+optional-freeze body (2101-2117) is dead and only the else warning (2118-2119) ever runs. Remove the whole if/else and the constant.
- **FREEZE_ENCODER dead branches** `training_scripts/human_activity_recognition/semantic_alignment_train.py:259,581-585,860-861,1509-1510,2111-2117` [safe] ~15LOC
  - why dead: FREEZE_ENCODER is hardcoded False (259) with no env override. `if FREEZE_ENCODER` bodies (581-585 no_grad encoder wrapper whose else 586-589 always runs; 1509-1510 encoder.eval(); 2111-2117 param-freeze block) never execute; `if not FREEZE_ENCODER` at 860-861 always runs (unconditional). Collapse to the unfrozen path. Low value but genuinely dead.
- **per_patch_prediction=False else-branches (training/validation side)** `training_scripts/human_activity_recognition/semantic_alignment_train.py:732-740,1015-1018,1046-1047,1249-1250,1256-1257,1298-1302,1361-1362,1385-1386,1443,1566-1567,1617-1618,1628-1631,1651-1652,1730-1732` [uncertain] ~60LOC
  - why dead: PER_PATCH_PREDICTION comes from config (253, default False) but ALL four config presets set per_patch_prediction=True (model/config.py:73,175,268,323) and all 15 ablations + headline hyperparameters.json record per_patch_prediction=true; no config sets it False (grep '"per_patch_prediction": false' across training_output = empty). So every `else`/non-per-patch arm (session-level criterion call 1015-1018; debug 1046-1047; window-by-sessions 1249-1257; cache 1298-1302; gradcache debug 1361-1362 & surrogate 1385-1386 & effective_neg ternary 1443; validate 1566-1567,1617-1618,1628-1631,1651-1652; evaluate_unseen 1730-1732; encode_raw non-per-patch fallback 732-740) is dead. Recommend hardcoding PER_PATCH_PREDICTION=True and collapsing all arms together, not piecemeal.
  - depends_on: ['semantic-head per_patch_prediction=False removal (model/semantic_alignment.py, other subagent)']
  KEEP/uncertain:
  - _train_epoch_gradcache (train 1193-1501) + SemanticAlignmentLoss.forward_cached (semantic_loss.py 488-523) + USE_GRAD_CACHE (277) + TARGET_EFFECTIVE_PATCHES (285) + dispatch 1518-1523: NOT used by any shipped/live config: USE_GRAD_CACHE is env-only (TSFM_GRAD_CACHE default '0'), no preset sets it, and headline + all 15 ablations record grad_cache=false. The sweep also found per_patc
  - SigLIPLoss branch in SemanticAlignmentLoss.__init__ (semantic_loss.py:437-438): Orchestrator: loss_type in {infonce,siglip} is a LIVE sweepable choice — do NOT remove either. BUT `class SigLIPLoss` is NOT DEFINED anywhere in the repo (grep -rn 'SigLIPLoss' => only the reference a
  - stage="stage1" default arg + "Stage 2" docstrings (train_epoch 1504, validate 1532, module docstring 1-5): Cosmetic Stage-1/Stage-2 naming vestige from the removed two-stage pipeline, but `stage` is load-bearing: it namespaces plot scalars (f'batch/{stage}_loss') and tqdm descriptions and is passed 'train'
  CONFIG updates:
  - semantic_alignment_train.py: delete TSFM_MEMORY_BANK / TSFM_MEMORY_BANK_SIZE handling (lines 278-279) and drop 'use_memory_bank'/'memory_bank_size' from the saved hyperparameters dict (line 2054)
  - semantic_alignment_train.py: delete PRETRAINED_ENCODER_PATH constant (line 258) and FREEZE_ENCODER constant (line 259) once their dead branches are removed
  - scripts/setup_runpod.sh:154 — remove the 'TSFM_MEMORY_BANK_SIZE=1024 — memory bank queue size' help line
  - scripts/generate_ablation_results.py — the summary string 'memory bank (512), no GradCache' must be updated to drop the memory-bank claim
  - scripts/run_ablations.sh:21 and scripts/setup_runpod.sh:153 — TSFM_GRAD_CACHE help lines: leave for now (GradCache kept as keep_uncertain); remove only if GradCache path is dropped
  - IF GradCache is later dropped: remove TSFM_GRAD_CACHE (277), TARGET_EFFECTIVE_PATCHES/TSFM_TARGET_PATCHES (285), 'use_grad_cache' in hyperparams (2059), the queue_diversity/'queue_diversity':0.0 return keys, and the config.py 'needs/Requires GradCache' comments (model/config.py:9,274)
  TEST updates:
  - tests/test_memory_bank.py — DELETE the whole file; it imports `from training_scripts.human_activity_recognition.memory_bank import MemoryBank` and is the only test of the removed module
  - tests/test_encoder_forward.py:159 (test_... multi-prototype label bank encoding, docstring line 4) — update/remove IF the multi-prototype label bank is removed (drives _forward_multi_prototype deadness; coordinate with the LearnableLabelBank subagent). Tests the label bank, not this loss.
  - tests/test_model_loading.py:106 test_multi_prototype_encode_shape and :131 test_multi_prototype_embeddings_normalized — same: update/remove with the num_prototypes>1 label-bank removal
  - tests/test_similarity_computation.py:22-39 (3D (L,K,D) label similarity) — eval-side multi-prototype; update if num_prototypes>1 support is dropped repo-wide
  - No test imports InfoNCELoss/SemanticAlignmentLoss or uses imu_queue/text_queue, so removing the memory-bank queue plumbing and _forward_multi_prototype breaks no loss-level test directly
  + verifier-found extra:
  - USE_ROTATION_AUGMENTATION (semantic_alignment_train.py:370) — dead DEPRECATED module-level constant ('# DEPRECATED (superseded by AUG_CONFIG.yaw_rotation)'). grep across the whole repo: defined once at line 370, ZERO other references (no reader anywhere). Pure dead legacy in the training area, missed by the classifier. Safe to delete the single line.
  - Legacy resume 'encoder' format branch (semantic_alignment_train.py:1911-1940, `elif 'encoder' in resume_hp:` labeled '# Legacy format (pre-config refactor): read from structured sections'). It restores pre-refactor CNN-tokenizer keys (CNN_CHANNELS, CNN_KERNEL_SIZES, TARGET_PATCH_SIZE) from the old structured hyperparameters layout. Every shipped/live hyperparameters.json uses the new `'config'` key (the `if 'config' in resume_hp` arm at ~1857), so this elif only fires for pre-config-refactor checkpoints that predate V2. Missed by the classifier. RISK: keep only if resuming very old runs matters; otherwise removable as legacy-format backward-compat (couples to the CNN/spectral tokenizer removal in model/, another subagent).
  - docs note only (not code): docs/v2/REPO_CLEANUP_PLAN.md:374 lists a `get_attention_stats` wrapper for removal in this file, but grep shows no `get_attention_stats` in semantic_alignment_train.py or semantic_loss.py today — already gone; no action, flagged so the classifier doesn't chase a phantom.

---

## Execution status (2026-07-10) — COMPLETE

All 5 waves executed on top of tag `v2-pre-legacy-purge`. Net **−2,601 LOC** (3,081
deleted / 480 added) across 41 files; 2 modules deleted; **145 tests pass**; the full
V2 stack (filterbank tokenizer + per-patch head + frozen mean-pool labels) composes
and runs end-to-end.

- **W1 augmentation** ✓ — deleted the old batch `IMUAugmentation` class, `yaw_rotation`/
  `YawRotationCfg`/`_rodrigues`, dead loader/train params, 10 legacy tests.
- **W2 training** ✓ — deleted `memory_bank.py` (whole MoCo module) + `test_memory_bank.py`,
  all queue plumbing (USE_MEMORY_BANK / warmup / enqueue / queue_diversity), the legacy
  pre-config-refactor resume branch, USE_ROTATION_AUGMENTATION. GradCache kept (live).
- **W3 semantic-head** ✓ — deleted the 9.3M dead session-level path (`TemporalAttention`,
  `MultiQueryPooling`, the `per_patch_prediction=False` branches) + dead head config keys;
  per_patch is now the only mode; added `per_patch_prediction:True` to SMALL_CONFIG.
- **W4 label-bank** ✓ — `LearnableLabelBank` is frozen mean-pool ONLY (0 trainable params);
  deleted `LabelAttentionPooling` + `MultiPrototypeLabelPooling`, `use_mean_pooling=False`
  branch, num_prototypes>1 support, ABLATION_LEARNABLE_LABEL_BANK / FREEZE_LABEL_BANK /
  label_bank_num_* keys; `model_loading.load_label_bank` now ignores legacy pooling.* weights.
- **W5 tokenizer/encoder/preprocessing** ✓ — deleted `SpectralTemporalExtractor`,
  `FixedPatchCNN`, `MultiScaleConv1D`, `ChannelIndependentCNN`, `interpolate_patches`,
  `normalize_patches`, and the legacy preprocessing branch; encoder now **raises** on any
  non-`physical_filterbank` type; every MODEL_SIZE preset resolves to filterbank via a
  shared `_FILTERBANK_KEYS` merge in `get_config`; `dft_size` default 256; also fixed the
  pre-existing `use_dc` loader-threading bug the audit surfaced.

**Deferred (not blockers):** a few dead-but-harmless `if is_filterbank:` /
`if dim()==3:` guards left in place (they always take the correct branch); the
`FREEZE_ENCODER` flag kept (still wired, always False); some empty `# --- Label bank ---`
comment stubs in config presets.
